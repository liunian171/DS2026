# BluePill_Car 工程文档

## 硬件平台

| 项目 | 详情 |
|------|------|
| MCU | STM32F103C8T6 (BluePill) |
| 主频 | 72MHz (HSE 8MHz + PLL ×9) |
| 工具链 | GCC + CMake + Ninja |
| HAL 库 | STM32Cube FW_F1 V1.8.7 |
| 串口 | USART2, 115200-8N1 (PA2 TX, PA3 RX) |
| I2C | I2C2: OLED (0x3C) + MPU6050 (0x68) |

## 引脚分配

| 引脚 | 功能 | 标签 |
|------|------|------|
| PA0 | TIM2_CH1 (编码器1 B相) | ENCODR1_B |
| PA1 | TIM2_CH2 (编码器1 A相) | ENCODR1_A |
| PA2 | USART2_TX | TX |
| PA3 | USART2_RX | RX |
| PA6 | TIM3_CH1 (编码器2 B相) | ENCODER2_B |
| PA7 | TIM3_CH2 (编码器2 A相) | ENCODER2_A |
| PA8 | TIM1_CH1 (电机A PWM) | PWMA |
| PA9 | TIM1_CH2 (电机B PWM) | PWMB |
| PB3 | GPIO 输入 (灰度1) | OUT1 |
| PB4 | GPIO 输入 (灰度2) | OUT2 |
| PB5 | GPIO 输入 (灰度3) | OUT3 |
| PB6 | GPIO 输入 (灰度4) | OUT4 |
| PB7 | GPIO 输入 (灰度5) | OUT5 |
| PB8 | TIM4_CH3 (舵机 PWM) | SERVO_PWM |
| PB10 | I2C2_SCL | - |
| PB11 | I2C2_SDA | - |
| PB12 | GPIO 输出 (电机A IN2) | AIN2 |
| PB13 | GPIO 输出 (电机A IN1) | AIN1 |
| PB14 | GPIO 输出 (电机B IN1) | BIN1 |
| PB15 | GPIO 输出 (电机B IN2) | BIN2 |
| PA13 | SWDIO (调试) | - |
| PA14 | SWCLK (调试) | - |

## 软件架构

```
Core/
├── Src/main.c                    ← 主入口，初始化 + 主循环
├── Src/common/                   ← 通用模块
│   ├── ringbuf.c/h               │  环形缓冲区
│   ├── imu_filter.cpp/h          │  IMU 互补滤波 + Raw转物理量 + 零偏校准
│   ├── tool.c/h                  │  工具函数 (handle_to_id)
│   └── pid.c/h                   │  PID 控制器
├── Src/driver/                   ← 驱动层 (ops抽象模式)
│   ├── encoder.c/h               │  编码器策略层
│   ├── encoder_platform_ops.c    │  STM32 编码器平台实现
│   ├── motor.cpp + bridge/protocol  │ 电机控制 (C++ 桥接)
│   ├── servo.cpp + bridge/protocol  │ 舵机控制
│   ├── pwm.c/h                   │  PWM 抽象层
│   ├── pwm_instance.c            │  PWM 实例定义
│   ├── pwm_platform_ops.c        │  STM32 PWM 平台实现
│   ├── uart.c/h                  │  UART 策略层
│   ├── uart_platform_ops.c       │  STM32 UART 平台实现
│   ├── uart_cmd_parser.c/h       │  串口命令解析（二进制协议）
│   ├── i2c_hardware_ops.c/h      │  I2C 硬件 ops（HAL 阻塞封装）
│   ├── mpu6050.cpp/h             │  MPU6050 芯片参数表
│   ├── imu_base.cpp/h            │  IMU 基类 (IIMU 接口实现)
│   ├── imu_bridge.cpp/h          │  IMU C 桥接层 + 改进互补滤波
│   ├── imu_uart_handler.cpp/h    │  IMU UART 命令处理器
│   ├── oled_driver.cpp/h         │  SSD1315 OLED 驱动
│   ├── oled_bridge.cpp/h         │  OLED C 桥接层
│   ├── oled_font.h / font_data.c │  字库 (8×16 ASCII + 16×16 汉字"流年")
│   ├── usergpio.c/h              │  GPIO 抽象层
│   ├── usergpio_platform.c       │  STM32 GPIO 平台实现
│   ├── useri2c.c/h               │  I2C ops 策略层
│   └── useri2c_ops.c             │  软件 I2C FSM 实现
├── Inc/main.h                    ← CubeMX 生成 (引脚宏定义)
├── Inc/dma.h / tim.h / usart.h   ← CubeMX 外设头文件
└── Src/dma.c / tim.c / usart.c   ← CubeMX 外设实现
```

## OLED 显示布局 (128×64, 4行×16列 8×16字体)

```
第0行: R:0.0 P:0.0        ← IMU Roll/Pitch
第1行: Y:0.0              ← IMU Yaw
第2行: E1:0 E2:0          ← 编码器1/2计数
第3行: CALIBRATE:99       ← 校准时倒计时 (前~25秒)
       或 G:10101          ← 校准完成后显示5路灰度 1/0
```

## 串口协议

### 双向二进制协议

**物理层**: 115200-8N1
**协议**: 自定义二进制帧

### 命令帧 (PC → STM32)

```
0xAA | cmd | [id] | [参数...] | 0xFF | 0xFF
 帧头   命令   可选ID   float/int  两字节帧尾
```

### 响应帧 (STM32 → PC)

```
0xAA | cmd | id | status | 0xFF | 0xFF
 帧头   命令   ID  状态码   两字节帧尾
```

### 命令列表

| 命令 | 码值 | 参数 | 说明 |
|------|------|------|------|
| CMD_NOP | 0x00 | 无 | 空操作 |
| CMD_MOTOR_SET_RPM | 0x01 | id + float(rpm) | 设置电机转速 |
| CMD_MOTOR_SET_MPS | 0x02 | id + float(mps) | 设置电机速度 |
| CMD_MOTOR_BRAKE | 0x03 | id | 刹车 |
| CMD_MOTOR_SET_DEADZ | 0x04 | id + float | 设置死区 |
| CMD_SERVO_SET_ANGLE | 0x10 | id + float | 舵机角度 |
| CMD_SERVO_START | 0x11 | id | 启动舵机 |
| CMD_SERVO_STOP | 0x12 | id | 停止舵机 |
| CMD_PWM_SET_DUTY | 0x20 | id + uint16 | PWM占空比(TODO) |
| CMD_IMU_GET_DATA | 0x30 | id | 请求IMU数据 |
| CMD_IMU_CALIBRATE | 0x31 | id + uint16 | IMU校准 |
| CMD_PING | 0xF0 | 无 | 心跳 |
| CMD_GET_STATUS | 0xF1 | 无 | 获取状态 |

### 状态码

| 码值 | 含义 |
|------|------|
| 0xA0 | OK |
| 0xA1 | 错误 |
| 0xA2 | 无效命令 |
| 0xA3 | 无效ID |
| 0xA4 | 无效参数 |
| 0xA5 | 忙 |

### 主动上行的调试数据

校准完成后，每 250ms 通过 `HAL_UART_Transmit` 主动输出一行文本：

```
IMU R:0.0 P:0.0 Y:0.0  ENC1:0 ENC2:0\r\n
```

## 初始化流程

```
HAL_Init()
  └→ SystemClock_Config (HSE+PLL=72MHz)
    └→ MX_GPIO_Init()
      ├→ MX_DMA_Init()
      ├→ MX_TIM1_Init()   (PWM 20kHz 电机)
      ├→ MX_TIM2_Init()   (Encoder 模式)
      ├→ MX_TIM3_Init()   (Encoder 模式)
      ├→ MX_USART2_UART_Init()  (115200)
      ├→ MX_TIM4_Init()   (PWM 50Hz 舵机)
      └→ MX_I2C2_Init()   (OLED + MPU6050)

User Code:
  ├── UART: ringbuf_init → uart_cmd_parser_init → uart_receive_IT
  ├── 编码器: encoder_start (TIM2/TIM3, PPR=1320)
  ├── 电机: pwm_set_freq → pwm_start → motor_bridge_init (×2)
  ├── 舵机: pwm_set_freq(50Hz) → servo_bridge_init
  ├── OLED: oled_bridge_init → oled_bridge_show_string("IMU TEST")
  └── IMU: imu_bridge_init(0, IMU_MPU6050, &imu_i2c)
          └→ i2c_hardware_ops → HAL_I2C_Mem_Write/Read
```

## 主循环 (250ms tick)

```
uart_cmd_parser_tick()          ← 串口命令解析

每 250ms:
  ├── imu_bridge_update_filter(0)
  │    ├── 前 100 次 (~25秒): 陀螺零偏校准
  │    └── 之后: 动态α互补滤波 + 运行中零偏漂移补偿
  ├── 读取编码器 + IMU 数据
  ├── 串口输出 (文本格式)
  └── OLED 刷新 (先清行再写入)
```

## IMU 滤波算法

### 初始校准 (前~25秒)
- 100 次采样取平均作为陀螺零偏
- 期间全信加速度计

### 运行中动态零偏补偿
- 检测静止 (三轴陀螺 < 1.5°/s 持续 > 2秒)
- 一阶低通滤波跟踪零偏漂移
- 时间常数: 静止 2-10秒 慢速跟踪，>10秒 加速跟踪

### 动态 α (加速度信任度)
- 静止 (|accel-1g|<8%): α=0.50
- 微动 (|accel-1g|<15%): α=0.25
- 轻度运动 (|accel-1g|<30%): α=0.10
- 剧烈运动: α=0.02
- dt > 1秒: α=1.0 (重置)

### Yaw
- 纯陀螺积分，会漂移（无磁力计）

## Motor 初始化参数 (待校准)

```c
motor_bridge_init(0, &pwm_tim1_ch1, ..., 200.0f, 33.1f);
motor_bridge_init(1, &pwm_tim1_ch2, ..., 200.0f, 33.1f);
```

- `200.0f`: 速度系数 (待标定)
- `33.1f`: 换算系数 (待标定)

## 当前 FLASH/RAM 占用

```
RAM:   3312 B / 20480 B (16.17%)
FLASH: 48216 B / 65536 B (73.57%)
```

## 关键文件索引

| 文件 | 说明 |
|------|------|
| Core/Src/main.c | 主程序入口 |
| Core/Inc/main.h | 引脚宏定义 |
| Core/Src/driver/imu_bridge.cpp | IMU 互补滤波 + 校准 |
| Core/Src/common/imu_filter.cpp | 滤波器核心计算 |
| Core/Src/driver/i2c_hardware_ops.c | I2C 硬件 ops (HAL 封装) |
| Core/Inc/driver/uart_cmd_parser.h | 串口协议定义 |
| cmake/gcc-arm-none-eabi.cmake | 工具链配置 |
| CMakeLists.txt | 顶层构建配置 |

## 已知问题 / TODO

- [x] 串口波特率: 原始 115200 ↔ 测试过 9600，已恢复 115200
- [x] 串口 `%f` 不显示: 已改为整数拆分，去掉 `-u _printf_float`
- [x] IMU 数据不更新: 硬件 I2C ops 已补
- [x] OLED 字符残留: 加入清行
- [ ] 电机速度系数待校准 (转一圈 + 走一米)
- [ ] PWM_SET_DUTY 命令未实现
