# DAPLink implements the CMSIS-DAP debug protocol.
source [find interface/cmsis-dap.cfg]
transport select swd

# Start conservatively. We can raise this after the physical link is stable.
adapter speed 1000

source [find target/stm32f1x.cfg]

# Do not require a dedicated NRST wire for the first connectivity test.
reset_config none
