# UNO R3球衡杆原型映射表

> 日期：2026-07-31；2026-08-01按张大头资料修订
> 目的：先在UNO R3完成视觉外环、张大头x42S角度接口和调参，再迁移到RM F427。
> 约束：不采用现有PID整定文档的控制结论或增益，只使用用户确认正确的系统参数。

> **当前安全状态：** 用户确认D2=`DIR`、D3=`PWM/STEP`，方向1=顺时针、0=逆时针；采用电脑串口桥接且不使用物理零位按键。K230脚本已部署为`/sdcard/main.py`并确认板级复位后自启动；COM7严格帧和COM7→COM9转发均已实测。重新采集空轨道背景并放回钢珠后，6秒228帧全部TRACK、位置-112 mm；锁定桥接264帧时UNO显示相同位置且pulse=0。尚未进入ZERO、参数设置、ARM或电机运动。

## 一、当前串级闭环

```text
目标位置 r（UNO保存）
        |
K230D -> 相对中心O的位置x、帧号、状态
        |
UNO: e=r-x -> 位置外环PID -> 有符号脉冲速率
        |
UNO: D3输出PUL/STEP，D2给出DIR，累计命令位置执行非对称硬限位
        |
x42S: 内部编码器 + Emm FOC电机位置闭环 -> 齿轮齿条 -> 梁
```

当前不使用AS5600，所以UNO不能直接测量实际梁角；累计脉冲只能表示命令电机轴角。X42S内部编码器能闭合电机轴，但齿隙、打滑和机构柔性仍不可观测。

## 二、职责映射

| 功能 | K230D | UNO原型 | x42S | 最终F427迁移 |
|---|:---:|:---:|:---:|---|
| 球位置视觉识别 | ✓ | 接收 | - | UART DMA/IDLE接收 |
| 目标位置保存 | - | ✓ | - | 迁移控制层变量 |
| 位置误差/外环PID | - | ✓ | - | 迁移PID接口与安全限幅 |
| 梁角到脉冲换算 | - | 待确认标准脉冲语义和实物细分后从零实现 | - | 迁移定时器PUL/STEP输出 |
| 电机位置内环 | - | - | 内部编码器+FOC | 保持在x42S内部 |
| x42S遥测 | - | 第一版不读 | 可提供 | F427 UART读取位置误差/状态等 |

## 三、冻结接口

K230D发送ASCII行：

```text
B,<frame_id>,<position_mm>,<state>\n
```

- `position_mm`：相对梁中心O，正右负左。
- `state`：0 LOST、1 VERIFY、2 TRACK、3 HOLD。
- 只有TRACK帧更新PID；LOST立即命令回水平；最后有效TRACK帧超过200 ms未更新也回水平。
- 当前`k230_try.py`已核对为4字段发送；新UNO解析器只按本4字段协议从零实现，不复用已弃用旧解析器。
- K230D当前使用UART2、38400、8N1，物理Pin 11为TX、Pin 12为备用RX；当前Pin 11未接UNO。实际`k230_try.py`已把同一严格`B,...`帧镜像到USB控制台，电脑桥接只过滤合法4字段帧并转发到UNO COM9。Windows当前枚举K230D为COM7，已由CanMV状态栏和实际严格帧双重确认。当前USB CDC按115200和12M主机配置均可读取，物理UART2仍保持38400。

## 四、当前UNO测试引脚与启动

| 引脚 | 当前功能 |
|---|---|
| D2 | x42S方向输出；实机确认1=顺时针、0=逆时针；用户确认从当前相机画面看顺时针使梁左端升高 |
| D3 | x42S PWM/STEP输出；一圈测试固定3200个计数脉冲 |
| D0/D1 | USB串口115200，接收电脑桥接的视觉帧和`ZERO`/参数/`ARM`/`DISARM`/`STATUS`命令 |
| 电脑桥接 | K230D USB镜像→电脑合法帧过滤→UNO COM9；用户已确认 |

当前PID程序不配置EN；上电D2/D3保持高阻，启动参数仍为0。只有人工调平并发送`ZERO`、参数安全门全部满足且存在200 ms内TRACK帧时，`ARM`才可能成功。首次受限测试已在运行时设置小动作参数并完成ARM/RETURN，测试结束后DISARM且端口关闭；运行时参数不等于写入启动默认值。

### 历史记录（不自动复用）

| 引脚 | 功能 |
|---|---|
| D2 | 人工调平确认按键，`INPUT_PULLUP`，按键接GND |
| D5 | x42S EN；上电保持高阻，因EN有效电平仍待实物确认 |
| D6 | x42S STEP |
| D7 | x42S DIR |
| D8 | K230D TX → UNO SoftwareSerial RX（38400） |
| D9 | SoftwareSerial占位TX，不连接K230D |

PID增益、脉冲速率、加速度、回零速率和`SIGN`初值均为0；人工调平后从电脑桥接控制台发送`ZERO`，再通过115200 USB串口逐项设置，满足全部安全门后`ARM`才会成功。

以上历史表中的D2按键及D5-D9来自已弃用旧实现，已被当前D2方向、D3 PWM/STEP接线覆盖。旧固件的`JOG+`、`JOG-`和`ARM`不得继续用于实物。

## 五、角度脉冲换算（标准脉冲模式的待实物确认换算）

### 当前PID机械硬限位

用户确认：以手动调平点为零位，顺时针最多2/3圈、逆时针最多1/3圈，超过可能影响水管。依据已确认的3200脉冲/圈，为确保整数脉冲不越界，新控制代码采用：

```text
顺时针命令位置上限 = +2133 pulse
逆时针命令位置下限 = -1066 pulse
```

该限位依据UNO累计发出的命令脉冲，并非X42S实际编码器位置反馈；当前脉冲方向接口不能向UNO返回实际位置。

已确认传动比为电机角到梁角`0.02885`。按x42S默认16细分、200整步/转：

```text
3200 pulse / 电机转
梁角/脉冲 = (360° / 3200) × 0.02885 = 0.003245625°
脉冲/梁角度 ≈ 308.106
±10° ≈ ±3081 pulse
```

该换算只在x42S配置为标准`PUL/STEP + DIR`闭环脉冲输入、实际为1.8°电机和16细分时成立。当前需读取实物菜单后才能用于代码；它不适用于`PUL_WID`模式。旧TMC2209的1/8细分数据同样不适用。

## 六、验证状态

- K230D脚本：4字段发送修改后通过Python AST语法检查；已保存为`/sdcard/main.py`，板级复位后自启动实测成功。
- UNO协议：主机回归测试通过。
- UNO固件：Arduino AVR `--warnings all`编译并烧录COM9成功，16198 B Flash（50%）、662 B SRAM（32%）。
- 启动验证：115200串口读回`WAIT_ZERO, armed=0, zero=0, pulse=0/0`；未按D2时`JOG+`被正确拒绝。
- 2026-08-01重新开发：旧烧录和测试结果不作为当前实现证据；新分支已建立无GPIO、无电机输出的安全空工程并编译通过。
- 2026-08-01一圈测试：D2/D3新源码按3200脉冲/圈编译通过，Flash 4806 B、SRAM 263 B，提交`73d180c`/`da7f158`；用户明确自行编译烧录，整圈尚未实测。
- 2026-08-01方向验证：用户反馈方向编号1为顺时针、0为逆时针，UNO子工程提交`a6a9e3f`；测试频率、准确一圈和自动停止尚未单独确认。
- 2026-08-01 PID准备阶段：K230D协议、目标0 mm和机械硬限位完成冻结；电脑桥接方案提交`3da5795`，当时源码尚未实现。该阶段状态已被下一条R07/R08实现结果覆盖。
- 2026-08-01 R07/R08软件实现：用户确认当前不使用物理按键，采用电脑`ZERO`。K230D USB镜像、电脑COM7→COM9严格帧过滤和UNO安全PID已实现；Python协议/转发测试10项通过，K230脚本AST通过，UNO编译Flash 12954 B（40%）、SRAM 457 B（22%），提交`31a8ae5`。未打开端口、未烧录、未输出脉冲。
- 2026-08-01首次只读联调：Arduino COM9读回新版固件`LOCKED/zero=0/frame=0/pulse=0`且参数全0；Agent关闭Arduino串口监视器释放COM9，未发送命令。CanMV确认COM7为K230D、约35–38 FPS、TRACK、位置约-11.16至-11.20 cm，但无严格B帧，且VS Code显示1个未保存文件；为避免覆盖用户内容暂停桥接，UNO记录提交`59174c0`。
- 2026-08-01启动与桥接实测：CanMV提示已保存`/sdcard/main.py`；固件v1.8-0的断开和Ctrl+D均未启动脚本，`machine.reset()`板级复位后18秒收到638个严格帧。COM7按115200/12M各3秒收到114/115帧。桥接统计输入352行、转发282帧，UNO帧号更新至3057并全程LOCKED、pulse=0，退出后DISARM；出现一次`ERR invalid_vision_frame`，原因待复测。UNO子工程提交`31b4e5a`。
- 2026-08-01空轨道背景实测：用户确认钢珠已移走；Ctrl+C确认进入K230 REPL后执行板级复位。日志从BG 57倒计数到4，随后明确输出`BG: 空轨道背景采集完成`并持续BG_OK；10秒收到341个严格state=0帧。COM9未打开，UNO和电机未操作；UNO子工程提交`ccad805`。
- 2026-08-01恢复TRACK实测：用户放回钢珠并确认视觉模块正常；COM7连续6秒228帧全部state=2、位置稳定-112 mm。第二次锁定桥接转发264帧，UNO帧号12451更新至12705，位置/state一致且全程LOCKED、zero=0、pulse=0。每次桥接各出现一次非法帧报告，原因待定位，未修改解析器；UNO子工程提交`71fe8ad`。
- 2026-08-01首次受限闭环：用户确认梁已调平且顺时针使画面左端升高，因此按当前误差定义采用`SIGN=+1`。运行时设置`KP=0.08`、`KI/KD=0`、`RATE=10`、`ACCEL=20`、`RETURNRATE=10`、`DEADBAND=3`；ARM 1.5秒累计+10命令脉冲，位置保持-112 mm，随后RETURN完成并回到pulse=0、LOCKED，最终DISARM并关闭端口。UNO子工程提交`38ab337`；数值仅为小动作安全测试，不是PID整定结论。
- 2026-08-01第二次PID动作：用户要求将位移测试上限放大至三分之一圈，按3200脉冲/圈取不越界的+1066命令脉冲。运行`SIGN=+1`、`KP=2`、`RATE=200`、`ACCEL=400`时，视觉由-112变为-114 mm触发保护；RETURN开始时UNO报告from_pulse=242，随后完成回零，最终DISARM并关闭端口。未到达+1066上限，也未使球向中心移动；UNO提交`16439ef`。
- 实物：R09的数据链、PID计算入口、电机输出、视觉监测、异常保护和命令回零已调通；控制效果、极性动态验证、I/D参数、中心稳定以及X42S实际编码器位置读取均未调通。x42S菜单实值仍未记录。
