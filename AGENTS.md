# AGENTS.md

> 本文件为Agent在本项目中的综合引导文件，包含行为规则、项目信息、器件清单和关键限制。

---

## 一、行为规则（必须遵守）

### 核心原则：不臆想，只写实事

1. **不要臆想**：文档中只能写入用户明确提供的信息或从可信资料中读取到的信息。不得根据通用知识自行补充具体规格参数（如电机电流、扭矩、尺寸等）。

2. **信息缺失时确认**：思考过程中如果发现信息缺失，必须在本次输出后向用户确认，不得自行填补。

3. **有歧义时确认**：如果用户的信息存在歧义或多解，必须向用户确认后再写入文档。

4. **写入前自检**：每次向文档写入信息前，检查该信息是否来自用户明确提供或资料读取。如果是Agent自行推测的，标注为"待确认"或直接不写入。

5. **ref仅供参考**：ref目录下的参考资料仅供参考，实际项目规划必须以用户明确给出的实事信息为准。

6. **不臆造器件规格**：器件的具体电气参数（电压/电流/扭矩/尺寸/PPR等）必须来自用户提供或手册读取，不得根据型号名推测。

7. **不臆造设计方案**：通信协议、状态机、速度规划、分工方案等设计内容，如果用户未明确提供，需向用户确认或标注为"建议方案(待确认)"。

8. **不臆造团队分工**：不自行分配团队成员任务，以用户明确给出的分工为准。

---

## 二、项目概述与目标

### 2.1 文档目的

本文件(AGENTS.md)是H题开发的**全局指导文件**，为后续接手的Agent提供：
- 项目背景与赛题要求的完整认知
- 已确认的器件清单与参数（避免重复确认）
- 系统架构与AB板分工
- 参考工程到目标平台的映射关系
- 开发流程与规范（移植顺序、测试、Git、文档）

本文件是**开发前的准备工作成果**，后续Agent应在本文档框架下进行开发。

### 2.2 项目背景

本项目为 2026 年全国大学生电子设计竞赛(TI杯赛区) H题开发工程。采用**双STM32架构**：A板(STM32F103ZET6)负责循线小车控制，B板(RM A型板 STM32F427IIH6)负责摆杆滚球控制。

目标赛题: **H题 - 车载平衡滚球运动控制系统**

### 2.3 开发目标

基于已有参考工程(`ref/BluePill_Car`、`ref/A_Board-Examples`、`ref/A_board_pwm_driver_test`)，为A板和B板开发完整的H题竞赛固件，实现：

1. **A板循迹小车控制**：红外循迹、电机速度闭环、计时显示、停车检测、向B板发送运动状态
2. **B板摆杆滚球控制**：步进电机角度控制、K230D球位置接收、球杆系统PID/LQR控制、运动前馈补偿
3. **图传装置**：ESP32-CAM无线图传到手机
4. **MCU间通信**：A板→B板有线UART传输运动状态

开发过程复用参考工程的驱动代码和控制逻辑，按自底向上顺序移植，逐模块测试验证。

---

## 三、目录结构

```
DS2026/
├── AGENTS.md                            # 本文件(综合引导)
├── doc/
│   ├── H题_原文.txt                     # 赛题原文
│   ├── 官方问题解答.txt                  # 官方Q&A(65问)
│   ├── 官方问题解答_补充2.txt             # 官方Q&A补充
│   └── H题设计/
│       ├── H题_任务详解与约束.md          # 逐项任务要求、场地规格、器件约束
│       ├── H题_器件选型方案.md            # 器件清单、AB板分工、TMC2209/ESP32-CAM规格、供电
│       ├── 确认器件参数手册.md            # ★核心: 所有确认参数集中管理
│       ├── A板_ZET6引脚映射与分配.md      # ★核心: A板ZET6物理引脚布局与外设分配
    │       ├── A板_循迹小车逻辑建模.md        # A板循迹控制: 运动学、控制架构、状态机
    │       ├── H题_硬件与代码程序思路流.md      # 系统硬件→代码→控制→通信全链路流程
    │       ├── B板_RM_A板资源与开发指南.md    # B板(F427)资源、样例分析、引脚规划
│       ├── A_Board_Examples源码总结.md    # RM A板官方样例源码分析(4个样例)
│       ├── ref参考资料总结.md            # ref工程/手册信息提取总结
│       ├── ESP32-CAM_规格书提取.txt       # ESP32-CAM规格原文
│       ├── 电机参数手册提取.txt           # 轮趣电机手册原文
│       ├── 17PM-J_手册提取.txt           # 步进电机手册原文
│       ├── RM_A板_用户手册_提取.txt       # RM A板手册原文
│       ├── RM_A板_使用说明提取.txt        # RM A板使用说明原文
│       ├── RM_OLED_使用说明_提取.txt      # RM OLED使用说明原文
│       └── 子工程索引.md                  # ★ 各子工程位置、Git、状态清单
└── ref/                                 # 仅供参考(236MB),实际以用户明确信息为准
    ├── A_Board-Examples/               # RM A板官方样例(4个:PWM/Imu/RM_OLED/RemoteControl)
    ├── BluePill_Car/                    # 完整循迹小车工程(F103C8T6,A板可参考移植)
    ├── A_board_pwm_driver_test/         # F427电机/串口/PWM架构(B板可参考)
    ├── 69028+ESP32-CAM.../              # ESP32-CAM资料+规格书+出厂固件
    ├── TMC2209/                         # TMC2209紫板产品图片
    ├── RM_databook/                    # RM A板手册PDF(4个)
    ├── 轮趣直流有刷电机...pdf            # 电机手册
    ├── Minebea 17PM-J.pdf              # 步进电机手册
    ├── A_Board_Examples_学习指南.md      # 样例学习指南(详细版)
    ├── A_Board_Examples_README.md
    ├── AS5600/                           # AS5600磁编码器资料
    ├── RM_A板_相关链接.md
    ├── CameraWebServer/                   # ESP32-CAM Web服务器固件(Arduino, 含WiFi配网+HTTP图传)
    └── 2026电赛E题&H题解题技术方案....md    # 达尔闻E+H题解题方案(含H题建模+控制+调试)
```

### 3.1 仓库定位与子工程关系

**本仓库（DS2026）是总规划仓库**，只存放设计文档和参考资料，**不存放具体代码**。

每个子工程独立维护自己的代码和仓库：

```
DS2026 (总规划/设计文档)
├── doc/H题设计/     ← 设计文档（本仓库的核心产出）
├── ref/             ← 参考资料（手册、开源方案、样例外）
├── AGENTS.md        ← 全局引导文件（本文档）
│
├──→ A板工程         位置: D:\HALcubemx\Apart_Board (CubeIDE)
│      独立的代码仓库，包含 doc/开发跟踪.md / 调试记录.md / 引脚映射表.md
│
├──→ B板工程         位置: (独立CubeIDE工程, 待创建)
│      独立的代码仓库
│
└──→ ESP32-CAM 工程  位置: C:\Users\27074\Documents\PlatformIO\Projects\ESP32 (PlatformIO)
       独立的代码仓库，包含 doc/开发跟踪.md / 调试记录.md / 引脚映射表.md
```

**各子工程详细位置和状态见** `doc/H题设计/子工程索引.md`。

**开发规范**：
- 开发前先读 `AGENTS.md` + 对应子工程的 `doc/开发跟踪.md`
- 代码只提交到子工程自己的 git 仓库
- 子工程完成阶段性成果后，回写进度到 `doc/H题设计/子工程索引.md`
- 本仓库 commit 粒度：每完成一个设计文档更新 → commit

---

## 四、开发环境

- **IDE**: CubeIDE for VSCode + CubeMX
- **调试器**: ST-Link
- **库**: HAL库
- **操作系统**: Windows

### A板 (循迹主控)
- **开发板**: STM32F103ZET6最小系统板
- **芯片**: STM32F103ZET6 (Cortex-M3, 72MHz, 512KB Flash, 64KB SRAM, 112 GPIO, 无FPU)
- **工程路径**: `D:\HALcubemx\Apart_Board` (CubeIDE/CubeMX工程)
- **板载独立接口**（用户确认）:
  - USB_TTL: CH340 USB转串口，占用USART1(PA9/PA10)，用于printf调试
  - USB_SL: STM32内置USB Device，占用PA11/PA12
  - R/Tx排针: 与USB_TTL共用PA9/PA10，板上直接丝印标注
- **物理排针**: 两侧各2列共4列（左侧=列1(外)+列2(内)，右侧=列3(内)+列4(外)），约119个排针位，引出109个GPIO
- **缺失GPIO**: PB3, PC14, PC15 (PB3未引出，PC14/PC15板载32kHz晶振)
- **引脚分配方案**（基于BluePill_Car参考，已解决PA9/USB_TTL冲突）:
  - 编码器1: PA0/PA1 (TIM2_CH1/CH2)
  - 编码器2: PA6/PA7 (TIM3_CH1/CH2)
  - 电机PWM: PC6/PC7 (TIM8_CH1/CH2) ← 避开PA9与USB_TTL冲突
  - 电机方向: PB12/PB13/PB14/PB15
  - 红外循迹: PB4-PB9
  - OLED I2C: PB10/PB11 (I2C2)
  - 调试串口: PA9/PA10 (USART1, USB_TTL)
  - B板通信: PA2/PA3 (USART2)
  - 启动按键: PC13
- **详细文档**: `doc/H题设计/A板_ZET6引脚映射与分配.md`

### B板 (摆杆控制主控)
- **开发板**: RoboMaster A型板
- **芯片**: STM32F427IIH6 (Cortex-M4F, 168MHz, 1MB Flash, 192KB SRAM, 带FPU)
- **板载**: MPU6500 IMU(SPI5), IST8310磁力计, SSD1306 OLED(SPI1), LED(PE11/PF14), 调试串口(USART6)
- **板载电源管理**: LM25116(12V@10A), MP2233(3.3V@3A), TPS54540(可调5~12V@5A), 支持4~6S LiPo
- **参考工程**: `ref/BluePill_Car/` (F103循迹小车完整工程) | `ref/A_board_pwm_driver_test/` (F427电机/串口/PWM架构)

### 构建与烧录
- **构建**: CubeIDE for VSCode中Build (CMake/Ninja)
- **烧录调试**: ST-Link SWD下载调试
- **外设配置**: CubeMX生成初始化代码(.ioc)
- 两板代码独立工程，各自编译烧录

### Git与网络
- **GitHub仓库**: https://github.com/liunian171/DS2026
- **网络代理**: 公司网络无法直连GitHub，需使用 **DevSidecar** 代理
  - DevSidecar默认代理端口: `127.0.0.1:31181`
  - Git代理配置命令:
    ```powershell
    git config --global http.proxy "http://127.0.0.1:31181"
    git config --global https.proxy "http://127.0.0.1:31181"
    ```
  - 推送完成后建议取消代理（避免影响其他工具）:
    ```powershell
    git config --global --unset http.proxy
    git config --global --unset https.proxy
    ```
  - 启动DevSidecar后等待代理就绪再执行git push/pull

---

## 五、系统架构 (H题 - 车载平衡滚球)

### A板 (循迹主控 STM32F103ZET6)
- 5~6路红外反射式传感器(间距23.27mm)循黑色环形路线(直线1.5m + 半圆弧r=0.5m)
- MG513XP28_12V电机PID速度/方向控制(TB6612驱动)，霍尔编码器13ppr AB相测速
- SSD1306 0.96" I2C计时显示屏(≤2英寸，装车载)
- 启动按键，停车精度≤2cm
- 向B板发送运动状态(UART有线)

### B板 (摆杆控制主控 RM A型板 STM32F427IIH6)
- 25cm PPR水管凹槽摆杆(铰链固定，h≥5cm)
- 美蓓亚17PM-J347-G2VS步进电机+TMC2209紫板控制摆杆角度(STEP/DIR脉冲，无UART)
- 庐山派Lite K230D(GC2093)检测球位置(UART通信)
- 球杆系统PID/LQR控制，接收A板运动状态做前馈补偿
- RM A板不挂在管子上(不用于检测摆杆倾角)

### 图传装置 (ESP32-CAM 独立)
- OV3660摄像头采集凹槽画面，WiFi图传(单向)
- 手机作为接收端，自带录屏

### MCU间通信
- A板 → B板 (UART有线): 小车速度/加速度/弯道标志/启停信号
- K230D → B板 (UART有线): 钢球位置坐标，≥30fps
- **必须有线通信**: 官方Q62确认测试期间仅允许图传无线工作

### 三端开发独立性分析

A板、B板、ESP32-CAM三者硬件独立、代码独立、可并行开发，但通过共用设计文档和UART有线协议互联。

**独立开发边界**:

| 单元 | MCU | 工程路径 | IDE | 独立功能 | 参考工程 |
|------|-----|----------|-----|----------|----------|
| A板 | F103ZET6 | `D:\HALcubemx\Apart_Board` | CubeIDE for VSCode | 循迹+电机控制+计时显示 | BluePill_Car |
| B板 | F427IIH6 | (独立CubeIDE工程) | CubeIDE for VSCode | 摆杆控制+球位置伺服 | A_board_pwm_driver_test / A_Board-Examples |
| ESP32-CAM | ESP32 | `ref/CameraWebServer/` | Arduino IDE | WiFi图传(独立烧录) | CameraWebServer |

**互联依赖关系**:

```
ESP32-CAM ──WiFi单向──→ 手机(录屏)
  (完全独立,不参与控制闭环)

A板 ──UART有线──→ B板
  (发送: 速度/加速度/弯道标志/启停信号)
  
K230D ──UART有线──→ B板
  (发送: 钢球位置坐标 ≥30fps)
```

- A板不依赖B板和ESP32-CAM，可单独测试循迹+电机+显示
- B板依赖A板提供运动状态做前馈补偿，但可先用模拟数据独立调试球杆控制
- ESP32-CAM完全不依赖A/B板，仅需WiFi→手机即可独立测试图传
- 联调时A板和B板需统一UART协议帧格式（参考BluePill_Car二进制协议: `0xAA | cmd | id | param | 0xFF 0xFF`）

**共用设计文档** (三端开发均需参考):

| 文档 | A板 | B板 | ESP32-CAM |
|------|:--:|:--:|:---------:|
| `确认器件参数手册.md` | ✓ | ✓ | ✓ |
| `H题_硬件与代码程序思路流.md` | ✓ | ✓ | ✓ |
| `H题_任务详解与约束.md` | ✓ | ✓ | |
| `A板_ZET6引脚映射与分配.md` | ✓ | | |
| `H题_器件选型方案.md` | ✓ | ✓ | |
| `ref参考资料总结.md` | ✓ | ✓ | ✓ |

---

## 六、已确认器件清单

| 模块 | 器件 | 主控 | 关键参数 |
|------|------|------|----------|
| A板 | STM32F103ZET6 | - | M3, 72MHz, 112 GPIO, 无FPU |
| B板 | RM A型板(STM32F427IIH6) | - | M4F, 168MHz, 带FPU, 板载MPU6500/OLED |
| 循迹 | 5~6路红外反射式传感器 | A板 | 间距23.27mm |
| 电机 | MG513XP28_12V | A板 | 12V, 减速比1:28, 霍尔编码器13ppr AB相, 轮径66mm(半径33mm) |
| 电机驱动 | TB6612 | A板 | 双路直流 |
| 视觉 | 庐山派Lite K230D (GC2093) | B板 | UART输出球位置 |
| 图传 | ESP32-CAM (OV3660) | 独立 | WiFi单向, 手机接收 |
| 摆杆执行器 | 美蓓亚17PM-J347-G2VS | B板 | NEMA17, 两相四线双极 |
| 步进驱动 | TMC2209国产紫板 | B板 | MAX 2A, 5.5-38V, MS1/MS2/MS3细分, STEP/DIR无UART |
| 显示屏 | SSD1306 0.96" I2C | A板 | ≤2英寸, 装车载 |
| 电池 | INR18650 4S2P (14.8V) | - | 车载供电 |
| 图传接收 | 手机 | - | 接收WiFi+录屏 |

> 器件详细参数见 `doc/H题设计/确认器件参数手册.md`

### 待确认参数
| 项目 | 说明 |
|------|------|
| 17PM-J347-G2VS电气参数 | J347未在手册中列出，需看标签/测电阻/问卖家 |
| TMC2209紫板Vref默认值 | 需万用表测量确认 |

---

## 七、三人分工

| 成员 | 职责 |
|------|------|
| 成员1 | 建模+规划+算法(球杆建模/PID/LQR/前馈/速度规划) |
| 成员2 | 底层移植(驱动代码/电机/编码器/UART/PWM/OLED移植) |
| 成员3 | 视觉识别(K230D球位置检测/ESP32-CAM图传) |

---

## 八、关键限制（官方问答确认）

- 测试期间**仅允许图传无线通信**，MCU间必须有线UART (Q62)
- 图传**纯单向**，禁止接收端回传控制指令参与平衡控制 (Q15)
- 摆杆**不允许任何改造**，不能开孔加装传感器 (Q65)
- 管壁**不允许**有任何检测及控制装置 (Q27)
- **不允许**电磁铁吸附钢球 (Q39)
- 钢球脱落即判定本次失败，**最多2次失败机会** (Q50+增补3)
- 误差**考察全程**，过渡过程不能超差 (Q37)
- 红外循迹**只许普通红外光电传感器**，不允许其他传感器 (增补2)
- 小车**不得调头行驶** (增补1)
- **不可使用接触式传感器**作为循迹模块 (增补4)
- 摆杆控制**可使用IMU辅助**检测倾角 (Q40-41)
- 小车上**图传装置无限制** (增补6)
- 设计报告画图工具: 国赛标准使用**Visio**

---

## 九、控制难点

- 球杆系统(ball-beam)为欠驱动非线性系统，需PID/串级/LQR控制
- 小车运动(加减速/转弯)与钢球位置存在耦合，需前馈补偿或运动规划
- 循线速度与球稳定性需平衡(整圈≤30s，AB段≤8s)
- 任意指定位置球稳定(误差≤1cm)需精确位置伺服

---

## 十、开发工作流程

### 10.1 文档驱动开发

1. **先读后写**：开发任何功能前，先阅读`ref/`中相关参考工程和`doc/H题设计/`中的设计文档，提取与当前功能相关的信息，更新到Markdown文档中供查阅。
2. **映射表先行**：在移植参考工程代码前，先编写自然语言映射表（不是代码），列出参考工程功能→目标平台的对应关系和差异。
3. **文档同步**：代码变更时同步更新`doc/H题设计/`下的设计文档。若设计有变动，先改文档再改代码。

### 10.2 自底向上移植顺序

参考工程到目标平台的移植，按以下优先级顺序进行：

| 优先级 | 层次 | 内容 | 参考来源 |
|--------|------|------|----------|
| P0 | 底层定义 | 引脚映射、外设配置(CubeMX)、时钟配置 | A_Board-Examples / BluePill_Car |
| P1 | 驱动层 | GPIO/UART/PWM/Timer/I2C/SPI驱动封装 | BluePill_Car/Core / A_Board-Examples/BSP |
| P2 | 器件层 | 编码器/TB6612/TMC2209/SSD1306/K230D通信 | BluePill_Car / A_Board-Examples |
| P3 | 控制层 | PID/巡线PD/球杆控制/速度规划 | BluePill_Car / 设计文档 |
| P4 | 应用层 | 状态机/任务调度/通信协议 | 设计文档 |

### 10.3 平台映射表（参考工程→目标平台）

开发前需编写映射表，记录参考工程功能与目标平台的对应关系：

| 参考工程 | 目标平台 | 需映射内容 |
|----------|----------|-----------|
| BluePill_Car (F103C8T6) | A板 (F103ZET6) | 引脚重映射(PWM TIM1→TIM8(PC6/PC7), 原因:PA9被USB_TTL占用)、外设重新配置(CubeMX)、OLED驱动适配(SSD1315→SSD1306)、编码器PPR(13ppr)、USART分工(USART1调试/USART2-B板通信)
| A_Board-Examples (F427IIH6) | B板 (F427IIH6) | 直接可用，需适配CubeIDE for VSCode工程结构(原为Keil MDK) |
| A_board_pwm_driver_test (F427) | B板 (F427IIH6) | 电机架构/UART协议/PWM驱动可直接复用 |

> 映射表需在`doc/H题设计/`下单独维护，每完成一个条目的移植就更新状态。

### 10.4 测试驱动

1. **逐模块测试**：每完成一个映射表条目的移植，编写对应的测试模块验证功能。
2. **单元联调**：每完成一个功能层(如驱动层全部完成)，进行单元联调测试。
3. **测试通过后才继续**：测试未通过不进入下一模块，修复后继续。
4. **无硬件时**：可编写逻辑测试(如PID算法输入输出验证、通信协议帧解析验证)，待硬件就绪后做实物测试。

### 10.5 工作条目完成后更新

每个工作条目完成后，必须进行以下文档更新：

1. **更新开发跟踪文档**：在工程目录的 `doc/` 下维护 `A板_开发跟踪.md`（A板）或对应跟踪文档，标记该条目为已完成，记录完成时间。
2. **记录调试问题**：将开发过程中遇到的问题、根因、解决方案写入 `doc/A板_调试记录.md`（或对应板），**每个问题都要写清楚现象、根因、解决方法**，防止后续开发再犯同样错误。
3. **更新设计文档**：根据实际实现情况，更新 `DS2026/doc/H题设计/` 下对应的设计文档（如引脚分配、驱动接口、通信协议等），确保文档与代码一致。
4. **更新映射表状态**：在平台映射表中标记该条目的移植状态（已完成/进行中/待开始），并记录实际与预期的差异。
5. **Git提交**：每完成一个条目+验证通过 → commit。提交信息写明模块名和验证状态。

### 10.6 文档体系维护

**必须维护的文档清单**（每个工程目录下）：

| 文档 | 路径 | 说明 |
|------|------|------|
| 开发跟踪 | `<工程>/doc/开发跟踪.md` | 记录各模块开发状态、完成时间 |
| 调试记录 | `<工程>/doc/调试记录.md` | 记录所有问题：现象→根因→解决 |
| 引脚映射 | `<工程>/doc/引脚映射表.md` | 引脚分配、外设配置 |

> 新工程启动时首先创建上述三份文档，并在首个commit中包含。

**子工程进度同步**（每个验证步骤后必做）：

每完成一个外设/模块的验证，必须在 `DS2026/doc/H题设计/子工程索引.md` 中同步更新该子工程的状态、已实现功能列表和待实现项。确保总规划仓库始终反映各子工程最新进展。

### 10.6 Git工作流

1. **版本管理**：项目使用git管理，善用commit和branch。
2. **提交粒度**：每完成一个映射表条目+测试通过 → commit。每完成一个阶段+联调通过 → commit。
3. **分支策略**：功能开发用feature分支，测试通过后合并到主分支。
4. **提交信息**：写明完成了哪个映射表条目/模块，测试结果如何。
5. **初始状态**：初始工作区应为干净的项目目录，开发过程中逐步添加代码。

### 10.7 确认机制

1. **开始前确认**：每开始一个新的开发阶段，先与用户确认细节，确认内容写入开发文档。
2. **有变动则更新**：后续如有变动，同步修改对应的开发文档。
3. **多使用提问**：遇到不确定的信息、方案选择、参数确认时，优先向用户提问而非自行决定。
4. **文档可追溯**：所有确认的决策都记录在`doc/H题设计/`下的文档中，确保可追溯。

### 10.8 代码质量要求

1. **合理注释**：代码需有合理的注释，说明功能、参数、返回值。
2. **文档完备**：最终成品代码需有配套文档，确保他人可容易进行二次开发。
3. **文档位置**：新增文档写在`doc/H题设计/`下，保持目录结构清晰。
4. **接口清晰**：模块间接口定义清晰，驱动层与控制层分离。

---

## 十一、竞赛时间节点

- 7月29日 08:00 开赛 → 8月1日 20:00 结束 (共3天)
- 设计报告需与实物一同封存提交
