# BallBeam_UNO 开发跟踪

## 当前阶段

- 重启分支：`codex/zdt-emm-restart`
- 已完成：R00 重新建立 Emm 自由协议开发基线。
- 已完成：R01 建立无电机输出的 Arduino 安全空工程。
- 历史工作：R03一圈测试已完成软件交付并取得方向反馈；当前源码已由R08安全PID固件替换。
- 当前硬件输出：用户已烧录R10三参数自动固件并实机ARM；UNO命令计数正常增长，但用户确认X42S电机轴本身几乎没有转动。
- 最新实机反馈：方向编号1为顺时针，0为逆时针；X42S菜单为`P_Pul=PUL_ENA`、`CtrlMode=CR_VFOC`、`En=HOLD`、`MStep=16`；准确一圈和自动停止结果尚未单独记录。
- 当前协议工作：用户已烧录UNO；K230脚本已保存为`/sdcard/main.py`并通过板级复位确认自启动。COM7严格帧、电脑过滤转发和UNO接收均已实测。
- 已按2026-08-01用户要求移除安全链：不再有TRACK超时/非TRACK回零和命令位置限位；`state>=1`即驱动电机；PID参数集中放在源码最前面。
- 零位方式：用户确认当前不使用物理按键；R10要求启动BAT前手动调平，打开COM9复位后把启动姿态定义为UNO命令零位。该零位不是X42S实际编码器或梁角反馈。
- 最新用户目标：从任意可识别初始球位置持续运行视觉位置PID并保持中心；以后只输入`KP/KI/KD`三个参数。R10按启动前手工调平、UNO复位自动定义命令零位、固定执行参数和`state>=1`自动ARM实现；安全链已全部移除。

## 阶段状态

| 编号 | 状态 | 结果/证据 |
|---|---|---|
| R00 | 已完成 | 文档完整性检查和 `git diff --check` 通过；见 R00 提交 |
| R01 | 已完成 | 旧实现已删除；关键字审计通过；UNO 编译为 Flash 1620 B、SRAM 188 B |
| R02 | 已完成 | D2=`DIR`、D3=`PWM/STEP`及实物`PUL_ENA/CR_VFOC/HOLD/MStep=16`均已由用户确认 |
| R03 | 已完成 | 一圈程序固定3200脉冲；方向/频率必须随RUN命令给出；编译Flash 4806 B、SRAM 263 B；源码已交付 |
| R04 | 部分硬件已验证 | DIR 1=顺时针、DIR 0=逆时针；其余整圈验收项待反馈 |
| R05–R06 | 等待接口确认/实机诊断 | 需确认`P_Serial`和通讯物理接口；优先只读X42S输入脉冲、目标/实时位置、位置误差和状态 |
| R07 | 已完成 | CanMV提示已保存`/sdcard/main.py`；`machine.reset()`后自启动，18秒收到638个严格帧；COM7在115200/12M均可读；桥接过滤转发实测通过 |
| R08 | 链路安全已实测 | 桥接统计352行、转发282帧；COM9帧号更新至3057，同时保持LOCKED、zero=0、pulse=0、参数全0；退出后DISARM；未ARM、未运动 |
| R09 | PID链路已动作，闭环效果未调通 | 第一轮+10脉冲时位置保持-112 mm；第二轮SIGN=+1、KP=2、RATE=200、ACCEL/RETURNRATE=400/200，拟以+1066为测试上限，但位置变为-114 mm触发保护，约+242脉冲即RETURN。最终pulse=0、LOCKED、zero=0、端口关闭；尚未把球移向中心或稳定 |
| R10 | 实机调试中 | 三参数自动ARM、state>=1驱动和200 pulse/s命令计数正常；实物菜单含MStep=16已确认，但1197命令脉冲时电机轴几乎不转。2026-08-01用户要求移除安全链（超时/非TRACK/触限回零），新固件为state>=1即驱动、无自动回零。等待X42S输入脉冲/位置诊断、双向调节和中心稳定验收 |
| R11 | 等待完成条件确认 | “每次执行后角度归零并清计数”尚未实现；建议以中心±5 mm连续500 ms为完成条件，待用户确认 |

## 重启声明

旧版 STEP/DIR 代码、`PUL_WID`代码、`JOG`、旧 PID、旧视觉协议及由这些实现形成的结论不进入本轮实现。当前重新选择脉冲方向接口时，也必须按 ZDT 资料和新工作条目从零实现。

每次操作的详细影响、验证和提交记录见 `操作日志.md`。

集中交接状态和程序路径见`当前联调总结.md`。
