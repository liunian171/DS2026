# BallBeam_UNO 操作日志

## 记录规则

每次可验收的工程操作必须记录：操作编号、对应工作条目、依据、影响范围、安全边界、验证结果和 Git 提交。操作开始时先登记，结束时补齐结果；未验证不得记为完成。

## O-001：重建 Emm 自由协议开发基线

- 日期：2026-08-01
- 对应条目：R00
- 状态：已完成
- 用户确认：采用 Arduino UART + Emm 自由协议；每次操作更新工作日志。
- 资料依据：DS2026 内两份 ZDT PDF 提取文档；ZDT `Emm_V5.c/.h` 原厂示例；用户明确提供的换算与脉冲参数。
- 影响范围：重写 README、工作流、工作条目、开发跟踪、调试记录、平台映射、引脚映射；新增本日志和 ZDT 资料映射。
- 安全边界：本操作不修改 Arduino 代码、不烧录、不配置引脚、不发送电机帧。
- 验证结果：8 份工程文档和 README 均存在；全文关键边界检索符合预期；`git diff --check` 通过；未修改 Arduino 源码。
- Git 提交：`R00 docs: restart on ZDT Emm UART baseline`（包含本日志的提交）。

## O-002：建立无电机输出的 Arduino 安全空工程

- 日期：2026-08-01
- 对应条目：R01
- 状态：已完成
- 操作依据：用户要求抛弃旧代码并以 ZDT 资料重新开发；R00 已完成并提交。
- 计划影响：替换 `BallBeam_UNO.ino`；删除旧 STEP/DIR、PID、视觉协议源码和对应旧测试；同步更新 README、工作条目和开发跟踪。
- 安全边界：新程序仅初始化 UNO USB 调试串口并输出状态文本；不配置任何 X42S/K230D/按键引脚，不创建电机通信串口，不发送任何协议帧；本操作只编译、不烧录。
- 验证结果：源码中未检出 `pinMode`、GPIO 写入、`SoftwareSerial`、串口帧写入或旧 STEP/DIR/JOG/PID/K230 路径；使用 Arduino IDE 内置 CLI 按 `arduino:avr:uno` 编译通过。Flash 1620/32256 B（5%），全局 SRAM 188/2048 B（9%）。未执行烧录。
- Git 提交：`R01 refactor: establish no-output Arduino baseline`（包含本日志的提交）。

## O-003：同步 R00/R01 状态到 DS2026 总仓库

- 日期：2026-08-01
- 对应条目：R01（总仓库进度同步）
- 状态：已完成
- 操作依据：DS2026 规定子工程阶段验证后更新 `doc/H题设计/子工程索引.md`。
- 影响范围：仅更新 DS2026 的 UNO 原型状态、Emm 自由协议选择、视觉输入边界、R02 工作门和本操作日志；未触碰总仓库内已有的其他未跟踪文件。
- 安全边界：只修改文档，不修改或烧录固件，不操作硬件。
- 验证结果：DS2026 目标文件 `git diff --check` 通过，并已单文件提交。
- Git 提交：DS2026 `fe444f4 docs: sync UNO Emm restart baseline`；本日志由后续 `R01 docs: record DS2026 progress sync` 提交记录。

## O-004：核对脉冲方向模式的闭环职责

- 日期：2026-08-01
- 对应条目：R02
- 状态：已完成（R02 仍等待实物配置确认）
- 用户新增确认：当前电机控制由 UNO R3 输出脉冲波和独立方向电平；每次工程操作继续更新工作日志。
- 资料依据：ZDT X42S 第二代闭环步进电机用户手册 V1.0.5、V2.0.2 固件更新记录、DS2026 的 ZDT PDF 提取件及当前源码审计。
- 计划影响：修正 README、工作流、工作条目、开发跟踪、调试记录、资料映射、引脚映射和平台映射；同步 DS2026 总仓库的 UNO 状态。
- 安全边界：本操作只做资料核对和文档重排；不修改 Arduino 源码、不烧录、不配置 X42S 引脚、不发送脉冲或通信帧。
- 待确认：用户所称“PWM 波”是否明确指固定有效脉宽的连续 `PUL/STEP` 脉冲列（频率/脉冲数控制运动），以及实物 `P_Pul`、`CtrlMode`、`En` 菜单当前值。确认前不恢复任何电机输出。
- 验证结果：9 份工程文档已同步；`git diff --check` 通过；源码 `BallBeam_UNO.ino` 无差异，当前程序仍无 GPIO 和电机输出；工作树未混入构建产物。
- Git 提交：`R02 docs: clarify X42S pulse closed-loop boundary`（包含本日志的提交）。

## O-005：同步脉冲闭环边界到 DS2026 总仓库

- 日期：2026-08-01
- 对应条目：R02（总仓库进度同步）
- 状态：已完成
- 操作依据：O-004 已完成资料核对并提交；DS2026 要求子工程设计变化同步总仓库。
- 影响范围：更新 DS2026 的全局规则、张大头控制基线、子工程索引、UNO映射、器件参数、对话确认记录及两份总览覆盖说明。
- 安全边界：只修改文档；未修改或烧录 UNO/X42S 固件，未配置引脚，未发送电机信号；DS2026 既有未跟踪文件未加入提交。
- 验证结果：DS2026 目标文档 `git diff --check` 通过；GitHub `origin/master` 已拉取并确认仍为 `f9b36bd`；同步提交成功。
- Git 提交：DS2026 `6396759 docs: sync X42S pulse closed-loop boundary`；本日志由 `R02 docs: record DS2026 pulse-boundary sync` 提交记录。

## O-006：用代码分级探测X42S实际配置与响应

- 日期：2026-08-01
- 对应条目：R02/R03
- 状态：已结束（未识别出旧映射下的有效输入模式）
- 用户授权：需要通过代码测试并观察结果确认脉冲语义、菜单相关行为和闭环响应；用户已说明硬件接线没有问题。
- 计划影响：先检查UNO串口和当前固件；随后建立默认无输出、必须逐级确认才能产生有限脉冲的诊断程序，编译烧录后记录原始串口输出和实物响应。
- 安全边界：不运行PID、不接入K230D闭环、不连续转动；程序上电默认锁定输出，运动测试限制脉冲数并允许立即停止。未能从软件或现有记录确定的准确引脚和反馈通道，不自行猜测后输出。
- 预期判定边界：单向`PUL/STEP + DIR`只能通过电机动作判断输入行为，不能直接返回`FWType`、实际编码器角、位置误差或堵转状态；若现有接线没有X42S→UNO反馈通道，则这些项目必须增加只读通信连接或由其他可观测信号确认。
- 验证结果：先确认COM9中的旧固件为`K230D_UART`且不驱动电机；随后烧录上电锁定诊断固件。未解锁时运动命令被拒绝。以D5/D6/D7诊断映射测试20 us标准脉冲累计80个；又测试1500/1510/1550 us、20 ms周期的有限脉宽帧，D5 EN分别为高阻、低、高。K230D约35 FPS观察时球坐标保持在-10.95至-11.00 cm，横杆无可判定位移。结束时串口状态为`armed=0`，控制引脚高阻。无动作不能证明具体菜单值或故障，因此未继续增大指令。
- 环境影响：为定时器式PWM诊断安装Arduino官方Servo 1.3.0；诊断源码随后被O-007新程序覆盖。
- Git提交：与O-007一并提交。

## O-007：按D2/D3新接线建立一圈测试程序

- 日期：2026-08-01
- 对应条目：R02/R03/R04
- 状态：已完成软件交付，等待用户硬件验证
- 用户确认：D2是方向引脚，D3是PWM引脚；先写一个使步进电机旋转一圈的程序。
- 实现：固定3200脉冲为一圈；上电D2/D3高阻；仅接受`RUN <dir:0|1> <frequency_hz>`后运行；频率需明确给出且测试软件限制为1至1000 Hz；`STOP`可中断；计满3200个脉冲后自动停止并恢复高阻。
- 安全边界：不设默认方向和默认频率，不上电自转，不接入PID/K230D闭环，不配置EN。1至1000 Hz只是测试程序安全限制，不写作X42S规格。
- 验证结果：`arduino:avr:uno --warnings all`编译通过，Flash 4806/32256 B（14%），SRAM 263/2048 B（12%）；`git diff --check`通过。烧录时COM9返回拒绝访问，随后串口也无法打开；因此新程序没有烧录、没有执行整圈，UNO仍保留上一版上电锁定诊断固件。
- 用户后续确认：由用户自行编译、烧录和实机观察；Agent不再操作COM9。烧录后使用115200串口发送`RUN <0|1> <Hz>`，并反馈方向、动作和停止结果。
- Git提交：`R03 feat: add D2/D3 one-revolution test`（包含本日志的提交）。

## O-008：记录D2方向编号的实机结果

- 日期：2026-08-01
- 对应条目：R04
- 状态：部分硬件已验证
- 用户反馈：测试结果与程序预期一致；方向编号1为顺时针，方向编号0为逆时针。
- 写实边界：本次只确认方向编号映射；用户未单独给出测试频率，也未在本轮明确回答是否准确转满一圈及是否自动停止，因此这些项目继续标为待验证。
- 影响范围：只更新README、工作条目、开发跟踪、引脚映射和操作日志；不修改源码，不操作串口或硬件。
- Git提交：`R04 docs: record verified direction mapping`（包含本日志的提交）。

## O-009：冻结K230D协议与PID机械安全边界

- 日期：2026-08-01
- 对应条目：R07/R08
- 状态：协议与安全边界已核对；等待UNO接收引脚确认
- 用户要求：编写PID调节程序，参数集中放在源码最前面，读取K230D串口位置并将钢珠保持在中心；从平衡点顺时针最多2/3圈、逆时针最多1/3圈；允许后续调用电脑辅助实测调参。
- 代码依据：当前 `C:\Users\Y_cheng\Desktop\DS_26\k230_try.py`。已核对其UART2为38400、8N1，物理Pin 11为TX、Pin 12为备用RX；发送帧为 `B,<frame_id>,<position_mm>,<state>\n`，相对中心位置单位为毫米，中心为0，状态2为TRACK。
- 限位换算：依据用户已确认的3200脉冲/圈，顺时针2/3圈为2133.33脉冲、逆时针1/3圈为1066.67脉冲。为保证不超过上限，控制代码拟采用相对手动调平点顺时针最大`+2133`、逆时针最小`-1066`脉冲的保守整数硬限位。
- 安全边界：本次只核对源码和记录设计边界，不修改电机源码、不烧录、不输出脉冲。PID初值不采用项目内PID整定文档结论；K230D接入UNO的数字引脚未确认，不猜测写入；位置误差到顺/逆时针校正的控制极性留待有限小动作实测。
- 静态验证：`k230_try.py` 已通过Python AST语法解析；帧生成语句、波特率、引脚和状态码均由当前源码直接读取。
- 待确认：K230D物理Pin 11（TX）当前实际连接到UNO的哪个数字引脚。
- Git提交：`R07 docs: freeze K230 protocol and PID limits`（包含本日志的提交）。

## O-010：确认双USB拓扑与PID数据通道缺口

- 日期：2026-08-01
- 对应条目：R07/R08
- 状态：拓扑已确认；通信路径待用户选择
- 用户确认：K230D与UNO没有串口信号线连接，二者只是分别连接到同一台电脑。
- 只读检查：Windows枚举Arduino Uno为COM9；同时枚举`OpenMV Cam USB COM Port`为COM7。COM7暂记为K230D候选端口，未打开、未发送数据，身份和实际输出仍待验证。
- 源码核对：当前`k230_try.py`把`B,<frame_id>,<position_mm>,<state>\n`写入K230D物理UART2 Pin 11；USB控制台只周期打印人类可读状态日志，并不原样输出每帧`B,...`控制数据。因此“同时插入同一电脑”本身不会让UNO收到球位置。
- 可选路径：A）增加K230D UART2 TX→UNO RX及共地，UNO独立闭环；B）保持双USB，让K230D向USB控制台镜像`B,...`帧，再由电脑桥接程序过滤并转发到UNO COM9。两种路径会改变固件结构与调参依赖，必须由用户确认后实施。
- 安全边界：本次只做设备枚举、源码读取和文档修正；未打开串口、未改源码、未烧录、未输出脉冲。
- Git提交：`R07 docs: record dual-USB communication gap`（包含本日志的提交）。

## O-011：确认采用电脑串口桥接

- 日期：2026-08-01
- 对应条目：R07/R08
- 状态：桥接路径已确认；等待零位接口确认
- 用户选择：采用电脑桥接，保持K230D和UNO分别通过USB接入同一台电脑。
- 冻结数据流：K230D将严格`B,...`帧镜像到USB控制台；电脑程序只过滤合法4字段帧并转发到UNO COM9；UNO在同一USB串口解析视觉帧和调参命令、返回状态。候选K230D端口为COM7，实际打开后仍需用帧内容验证身份。
- 实现边界：电脑桥接只转发感知数据和用户明确输入的调参命令，不自行生成PID参数或电机动作；端口断开、视觉超时和非法帧都必须使UNO退出主动闭环。
- 新发现的确认门：此前用户要求手动调平后按键确认，但D2现在是X42S方向输出，尚无新的按键引脚。实现前需由用户确认改用电脑命令`ZERO`，或提供新按键引脚。
- 安全边界：本操作只更新设计记录；未改源码、未打开串口、未烧录、未输出脉冲。
- Git提交：`R07 docs: confirm PC serial bridge architecture`（包含本日志的提交）。

## O-012：实现电脑桥接与上电锁定PID固件

- 日期：2026-08-01
- 对应条目：R07/R08
- 状态：软件已验证，待用户烧录和端口实测
- 用户确认：当前不需要物理零位按键；本阶段使用电脑命令`ZERO`确认人工调平零位。
- 计划实现：在当前K230D脚本中镜像严格4字段控制帧到USB；在UNO工程新增COM7→COM9桥接程序及协议测试；从零替换一圈测试源码为视觉PID固件。
- 参数布局：PID增益、目标、死区、数据超时、速率、加速度、回零速率、方向极性和日志周期集中放在UNO源码最前面。未经实测的增益、速率、加速度及控制极性初值均设为0，`ARM`必须检查这些安全门，避免编译烧录后自动运动。
- 固定安全边界：上电D2/D3高阻；D2高=顺时针、低=逆时针；目标0 mm；只接受TRACK；相对电脑`ZERO`点顺时针不超过+2133脉冲、逆时针不超过-1066脉冲；视觉非法、非TRACK或超时进入命令零位返回；`DISARM`立即停止并使零位失效。
- 实机边界：本操作只编写并离线验证代码，不烧录、不占用COM7/COM9、不输出电机脉冲。编译通过后仍由用户烧录；首次实机先只验证视觉帧和状态，设置非零参数及`ARM`前另行确认。
- 实现结果：当前K230D脚本新增严格4字段帧USB镜像；UNO工程新增`tools/pc_serial_bridge.py`、`requirements.txt`和10项协议/转发测试；UNO一圈源码从零替换为电脑ZERO、安全门、视觉PID、速率斜坡、非对称脉冲限位及失效回零固件。
- 验证结果：桥接协议/转发10项测试通过；桥接`--help`通过；项目`.venv`安装并导入pyserial 3.5；K230D脚本通过Python AST；UNO按`arduino:avr:uno --warnings all`编译通过，Flash 12954/32256 B（40%），SRAM 457/2048 B（22%）。编译警告仅来自Arduino AVR Core自带`new.cpp`的未使用参数，不来自本工程源码；`git diff --check`通过。
- 未验证项：未打开COM7/COM9，未烧录新版UNO，未确认COM7实际输出镜像帧，未设置非零PID/速率/加速度/极性，未输出电机脉冲。
- Git提交：桥接与PID三端代码为同一联调单元，合并提交`31a8ae5 R07-R08 feat: add PC-bridged safe PID control`。

## O-013：提交R07/R08并同步DS2026总仓库

- 日期：2026-08-01
- 对应条目：R07/R08（软件验证与总仓库同步）
- 状态：已完成
- UNO提交：`31a8ae5 R07-R08 feat: add PC-bridged safe PID control`。
- DS2026同步：更新UNO子工程状态、串级映射和对话确认记录；明确电脑`ZERO`、所有未实测参数为0、10项桥接测试、K230 AST、UNO编译占用及尚未进行的实测项。
- DS2026提交：`b39dc9c docs: sync UNO PC-bridged PID implementation`。
- 外部源码说明：`C:\Users\Y_cheng\Desktop\DS_26\k230_try.py`所在目录不是Git仓库；该文件的USB镜像改动已通过AST和文本契约检查，并在UNO及DS2026日志写明，未伪造外部提交。
- 安全边界：同步过程只提交文档；DS2026既有未跟踪文件未加入提交，COM7/COM9仍未打开，硬件仍未操作。
- Git提交：`R08 docs: record DS2026 PID implementation sync`（包含本日志的提交）。

## O-014：首次LOCKED状态电脑只读联调

- 日期：2026-08-01
- 对应条目：R07/R08/R09
- 状态：UNO启动安全已验证；K230D镜像联调等待未保存文件处理
- 操作方式：按用户授权使用computer-use读取CanMV与Arduino IDE可访问状态；未打开桥接串口、未发送串口命令。
- K230D证据：CanMV v0.9.6状态栏显示COM7、K230D Lushanpi Lite 128M、固件v1.8-0、正在预览；终端持续约35–38 FPS，`STATE:TRACK`，位置约-11.16至-11.20 cm。输出未出现`B,...`镜像；VS Code显示1个未保存文件，不能擅自覆盖。
- UNO证据：Arduino IDE串口监视器COM9/115200持续输出`mode=LOCKED zero=0 frame=0 pulse=0`，`kp/ki/kd/sign/rate/accel/return_rate`全部为0，证明用户已烧录新版安全PID固件且当前无运动命令。
- UI操作：通过Arduino IDE的串口监视器快捷键关闭监视器以释放COM9；没有向UNO发送文本。CanMV保持原状，未保存、未停止、未断开。
- 安全结果：未发送`ZERO`、参数、`ARM`或任何电机脉冲；电机保持锁定。由于K230未输出严格控制帧，未启动电脑桥接。
- 下一步确认：用户先保存VS Code中的未保存文件并反馈；随后重核磁盘镜像补丁并重新运行K230D，再释放COM7进行桥接。
- Git提交：`R07-R08 docs: record first locked desktop check`（包含本日志的提交）。
- 总仓库同步：DS2026已同步本次UNO启动证据、COM7实际身份和未保存文件阻塞，提交`a1f33d9 docs: sync first locked UNO desktop check`；DS2026既有未跟踪文件未加入提交。

## O-015：部署K230启动脚本并完成LOCKED电脑桥接实测

- 日期：2026-08-01
- 对应条目：R07/R08/R09
- 状态：R07完成、R08链路安全完成；R09等待现场背景采集。
- 用户确认：未保存内容已经处理，可以继续；仍按此前约束不发送`ZERO`、非零PID/速率参数或`ARM`。
- K230部署：通过CanMV v0.9.6把当前`k230_try.py`保存为`/sdcard/main.py`，界面明确提示保存成功。CanMV断开和REPL Ctrl+D均未启动脚本；随后对K230D Lushanpi Lite固件v1.8-0执行一次`machine.reset()`板级复位，启动脚本自动运行。
- K230证据：复位后18秒读取799行，其中638行为严格`B,<frame>,<position>,<state>`帧；启动日志包含60帧背景采集提示。随后6秒稳态采样229帧全部为state=0。COM7分别按115200和12M各读取3秒，得到114/115个严格帧，当前USB CDC两种主机配置均可读。
- 桥接证据：短时运行COM7→COM9过滤转发，统计K230输入352行、转发282帧、拒绝70行人类可读日志。UNO帧号从0更新至3057，始终`mode=LOCKED zero=0 pulse=0`，PID、方向和三个速率参数全部为0；退出时收到`DISARM reason=user`。全过程未产生电机运动。
- 待复核现象：UNO在桥接期间输出一次`ERR invalid_vision_frame`；当前样本不足以确认根因，不修改解析器。自动启动后的state=0也不直接定因，需现场按60帧提示移走钢珠后重测TRACK。
- 安全边界：未发送`ZERO`、`KP/KI/KD`、`SIGN`、`RATE`、`ACCEL`、`RETURNRATE`或`ARM`；D2/D3保持锁定，累计pulse=0。COM7和COM9在测试结束后均已关闭。
- 下一步：现场操作者准备好后，移走钢珠并复位K230完成背景采集，再放回钢珠；先验证连续TRACK和位置随钢珠变化，之后另行确认人工调平、ZERO及受限控制极性测试。
- Git提交：`R07-R08 docs: record locked bridge validation`（包含本日志的提交）。
- 总仓库同步：DS2026已更新UNO映射、子工程索引和对话确认记录，提交`8bc39bb docs: sync locked K230-UNO bridge validation`；既有未跟踪文件未修改、未暂存。

## O-016：钢珠移出后的K230空轨道背景采集

- 日期：2026-08-01
- 对应条目：R09前置视觉准备
- 状态：空轨道背景采集已完成，等待用户放回钢珠。
- 用户现场确认：钢珠已经移走；未确认放回前不进入TRACK判断或闭环。
- 操作：只打开COM7。先向当前独立运行脚本发送一次Ctrl+C，3秒内观察到`用户中断`和`>>>`，确认进入REPL；随后执行一次已验证的`machine.reset()`板级复位。未打开COM9，未向UNO发送任何内容。
- 实测证据：K230启动日志从`BG:57`逐步倒计数至`BG:4`，随后明确输出`BG: 空轨道背景采集完成`；之后多条日志持续包含`BG_OK`。10秒共收到341个严格控制帧，帧号1至341，全部state=0，符合当前空轨道无钢珠阶段。
- 安全边界：COM7读取结束后已关闭；K230继续独立运行。未发送`ZERO`、PID参数、方向、速率或`ARM`，未打开COM9，电机没有动作。
- 下一步：用户把钢珠放回水管且不移动相机、水管和光照；随后先只读COM7验证是否恢复连续TRACK及位置毫米值。
- Git提交：`R09 docs: record empty-beam background capture`（包含本日志的提交）。
- 总仓库同步：DS2026已更新UNO映射、子工程索引和对话确认记录，提交`ecd215f docs: sync K230 empty-beam background capture`；既有未跟踪文件未修改、未暂存。

## O-017：钢珠放回后的TRACK与锁定桥接验证

- 日期：2026-08-01
- 对应条目：R09视觉输入与串级链路前置验证
- 状态：视觉TRACK及LOCKED桥接已完成；等待人工调平与机械方向映射确认。
- 用户现场确认：钢珠已经放回，并明确视觉模块没有问题。
- COM7只读证据：6秒读取228个严格帧，全部state=2；首帧11466、末帧11693，帧号差227且连续；位置最小、最大和平均值均为-112 mm。
- 锁定桥接证据：短时COM7→COM9输入330行、转发264帧、拒绝66行人类可读日志。UNO首个状态为frame=12451、末个状态为frame=12705，均为pos_mm=-112、vision_state=2、mode=LOCKED、zero=0、pulse=0，PID/方向/速率参数全0；进程正常退出。
- 待定位现象：第二次锁定桥接再次出现一次`ERR invalid_vision_frame`，与第一次相同；当前只记录复现，不臆定根因或修改代码。其余帧持续正常更新。
- 安全边界：未发送`ZERO`、PID参数、`SIGN`、速率或`ARM`；电机未动作。测试完成后COM7/COM9均关闭，桥接退出路径发送DISARM。
- 下一步确认：用户手动把梁调到水平并确认；同时需明确从同一观察方向看，电机顺时针会使梁左端升高还是右端升高。未确认这两项前不选择SIGN或首次运动参数。
- Git提交：`R09 docs: record TRACK locked-bridge validation`（包含本日志的提交）。
- 总仓库同步：DS2026已更新UNO映射、子工程索引和对话确认记录，提交`e9c247a docs: sync K230 TRACK locked-bridge validation`；既有未跟踪文件未修改、未暂存。

## O-018：确认人工水平与首次控制极性

- 日期：2026-08-01
- 对应条目：R09首次运动前确认门
- 状态：水平零位姿态和控制极性已确认；等待用户批准首次受限运动参数。
- 用户现场确认：梁已手动调平；从当前相机画面观察，电机顺时针会使梁左端升高。
- 符号核对：视觉定义负值在左侧；当前位置为-112 mm，位置误差`0-(-112)`为正。固件注释和实现均定义正请求速率为顺时针、D2高为顺时针，因此`SIGN=+1`会先使左端升高，方向指向中心。
- 零位边界：本次未发送`ZERO`。桥接退出会发送DISARM并使零位失效，所以ZERO必须在用户批准首次运动后，于同一桥接会话中紧接参数设置和受限ARM测试。
- 建议方案（待用户确认，尚未执行）：`SIGN=+1`、`KP=0.08`、`KI=0`、`KD=0`、`RATE=10 pulse/s`、`ACCEL=20 pulse/s²`、`RETURNRATE=10 pulse/s`、`DEADBAND=3 mm`；ARM最多1.5秒，预计最多约15个命令脉冲。若位置向更负方向变化2 mm立即DISARM；若向中心改善3 mm或到时则RETURN回命令零位。以上数值是本次小动作安全上限建议，不是PID整定结论。
- 安全边界：本操作只读核对源码并更新文档；COM7/COM9未打开，未ZERO、未设置参数、未ARM，电机没有动作。
- Git提交：`R09 docs: confirm level and control polarity`（包含本日志的提交）。

## O-019：首次受限视觉闭环动作

- 日期：2026-08-01
- 对应条目：R09首次受限运动
- 状态：软件与串口证据已完成；等待用户确认现场动作观感。
- 执行参数：`SIGN=+1`、`KP=0.08`、`KI=0`、`KD=0`、`RATE=10 pulse/s`、`ACCEL=20 pulse/s²`、`RETURNRATE=10 pulse/s`、`DEADBAND=3 mm`；这些数值仅用于首次小动作，不作为PID整定结论。
- 前置检查：COM7→COM9转发46帧后确认最新帧为TRACK、位置-112 mm；各参数均收到UNO的`OK`回应，随后在同一会话发送`ZERO`，状态确认`mode=LOCKED zero=1 pulse=0`。
- 动作证据：发送`ARM`后请求速率为+8.96 pulse/s；1.5秒内累计命令位置由0增至+10脉冲，视觉位置持续-112 mm，未触发“恶化2 mm”保护。到时发送`RETURN`，UNO从+10脉冲反向返回并明确输出`RETURN complete; output_locked; zero_retained=1`。
- 结束证据：回零后状态为`mode=LOCKED zero=1 pulse=0`；随后发送`DISARM`，最终状态为`mode=LOCKED zero=0 pulse=0`。本次电脑侧共读取K230 310行，转发248个严格帧，拒绝62行日志；COM7和COM9均已关闭。
- 结论边界：只证明UNO确实执行了+10命令脉冲并按累计命令位置返回0，且该时间窗内视觉位置没有变差；不能仅凭串口证据确认肉眼可见幅度、梁实际角度或X42S编码器实际位置。下一次增大动作前需用户反馈现场看到的动作和回平情况。
- Git提交：`R09 test: record first bounded closed-loop motion`（包含本日志的提交）。
- 总仓库同步：DS2026已更新UNO映射、子工程索引和对话确认记录，提交`75909fe docs: sync first bounded UNO closed-loop test`；既有未跟踪文件未修改、未暂存。

## O-020：放宽至三分之一圈上限的第二次PID实测

- 日期：2026-08-01
- 对应条目：R09 PID实机调试
- 用户指令：把位移放大到三分之一圈并先进行一次测试；随后要求先汇总当前开发情况。
- 测试定义：依据已确认的3200脉冲/圈，取不超过三分之一圈的`+1066`命令脉冲作为本轮上限；使用真实K230视觉帧，不伪造位置。运行参数为`SIGN=+1`、`KP=2`、`KI/KD=0`、`RATE=200 pulse/s`、`ACCEL=400 pulse/s²`、`RETURNRATE=200 pulse/s`、`DEADBAND=5 mm`。这些是单次测试参数，不是PID整定结论。
- 前置证据：桥接稳定TRACK，基线位置-112 mm；参数、ZERO均收到UNO确认，ARM成功。
- 动作结果：UNO正向加速至200 pulse/s；状态记录依次为+1、+22、+65、+112、+160、+207脉冲。视觉位置先为-112 mm，后为-113 mm并达到-114 mm，触发外部“较基线恶化2 mm”保护；发出RETURN时最新周期状态为+207脉冲，UNO随后报告`RETURN begin ... from_pulse=242`，因此本轮没有运行到+1066脉冲上限。
- 回零与安全结果：UNO从+242命令脉冲反向返回，明确输出`RETURN complete; output_locked; zero_retained=1`；随后STATUS为`mode=LOCKED pulse=0`，再DISARM使`zero=0`。最终视觉回到-112 mm附近，COM7/COM9已关闭，无测试进程运行。
- 结论边界：PID数据链、ARM、正向脉冲、视觉监测、保护RETURN和命令回零均实际工作；钢珠未向中心移动，不能称为PID调通。-112到-114 mm只有2 mm变化，当前不足以区分控制极性、视觉噪声、机械响应延迟或其他原因，不据此擅改SIGN。
- Git提交：`R09 test: record second PID motion and return`（包含本日志的提交）。
- 总仓库同步：DS2026已同步第二次PID动作结果和当前调通边界，提交`eb1b44e docs: sync second UNO PID motion test`；既有未跟踪文件未修改、未暂存。

## O-021：新增一键启动电脑桥接的BAT

- 日期：2026-08-01
- 对应条目：R07/R08电脑桥接操作辅助
- 状态：脚本已创建并完成无硬件错误路径验证，等待用户实际双击运行。
- 用户需求：将“检查COM7/COM9、释放串口后启动K230D到UNO电脑桥接”的操作整理为一个BAT文件，避免只运行`k230_try.py`时UNO因没有电脑转发而持续显示`frame=0`。
- 实现结果：新增工程根目录`start_ballbeam_bridge.bat`。默认使用K230D `COM7`和UNO `COM9`，也允许用第一、第二个命令行参数覆盖端口；启动前检查项目`.venv`、`tools/pc_serial_bridge.py`和两个串口是否存在，并提示关闭CanMV串口连接及Arduino串口监视器；确认后启动现有桥接程序。O-027后默认不显示K230人类可读调试日志，避免持续刷屏阻碍命令输入。
- 安全边界：BAT不自动发送`ZERO`或`ARM`。打开COM9通常会复位UNO，操作者必须先确认`frame`持续增加且`vision_state=2`，再手工调平并从桥接控制台输入`ZERO`；退出桥接时沿用Python程序的最佳努力`DISARM`路径。
- 验证结果：使用不存在的`COM999`执行无硬件测试，BAT正确识别端口不存在、显示错误说明并退出；`git diff --check`通过。验证过程未打开COM7/COM9、未复位UNO、未发送控制命令、未输出电机脉冲。
- Git状态：BAT和本次日志更新尚未提交。

## O-022：诊断运行K230脚本后UNO仍无新帧

- 日期：2026-08-01
- 对应条目：R07/R08电脑桥接联调
- 状态：已定位当前直接原因，等待用户释放串口并启动BAT桥接。
- 用户现象：每次连接K230D后都运行`k230_try.py`，但UNO仍无新反应；此前状态持续为`frame=0`、`vision_state=0`和`mode=LOCKED`。
- 只读检查：Windows当前正常枚举K230D为`COM7`、Arduino UNO为`COM9`；CanMV后台和Arduino IDE进程正在运行；没有发现任何Python或`pc_serial_bridge.py`桥接进程。
- 结论：运行K230D脚本只会让控制帧出现在K230D物理UART2和COM7，两个独立USB串口不会由Windows自动互通。当前没有电脑桥接进程把COM7帧转发到COM9，因此UNO保持`frame=0`是预期安全结果。CanMV和Arduino IDE进程存在，但是否各自仍打开串口监视窗口没有通过本次进程枚举单独确认。
- 后续操作：先确保`k230_try.py`已作为`/sdcard/main.py`独立运行，再断开CanMV串口并关闭Arduino串口监视器，随后双击`start_ballbeam_bridge.bat`；桥接窗口出现后先确认UNO的`frame`持续增加且`vision_state=2`，再手工调平并输入`ZERO`。不得在视觉帧尚未到达时发送`ARM`。
- 安全边界：本次只读取设备枚举和进程信息；未打开COM7/COM9、未复位UNO、未发送串口命令、未输出电机脉冲。
- Git状态：本次仅追加操作日志，尚未提交。

## O-023：澄清释放COM7与停止K230程序的区别

- 日期：2026-08-01
- 对应条目：R07/R08电脑桥接启动顺序
- 状态：已说明正确操作边界，等待用户确认K230独立运行状态。
- 用户反馈：已先让K230在COM7停下。
- 关键澄清：电脑桥接需要释放CanMV对COM7的占用，但K230D板上的识别程序必须继续运行。如果在CanMV中按“停止”使程序回到REPL，`B,<frame>,<position>,<state>`控制帧也会停止，桥接即使成功打开COM7仍然没有可转发数据。
- 正确顺序：先把当前`k230_try.py`保存为`/sdcard/main.py`，再通过已经验证的板级复位或重新上电使其独立自启动；确认脚本运行后只断开CanMV串口连接以释放COM7，不停止板上程序；随后关闭Arduino串口监视器并运行`start_ballbeam_bridge.bat`。
- 安全边界：本次只提供操作澄清并更新日志；未连接串口、未操作K230D、未复位UNO、未发送电机命令。
- Git状态：本次仅追加操作日志，尚未提交。

## O-024：记录K230停止后数秒再次运行的现象

- 日期：2026-08-01
- 对应条目：R07/R08 K230独立启动与COM7释放
- 状态：现象已记录，等待通过断开CanMV并运行桥接确认是否为板上`main.py`独立自启动。
- 用户反馈：在CanMV中点击停止后，数秒后K230程序又继续运行。
- 判断边界：该现象与`/sdcard/main.py`自动启动或CanMV侧再次触发运行均相容，仅凭现象不能确定是哪一方重新启动。当前无需继续反复点击停止；是否真正独立运行应以断开CanMV后，桥接程序能否从COM7连续收到严格`B,...`帧为验收证据。
- 建议操作：保持K230当前运行，不再点击停止；使用CanMV的断开设备/断开串口功能，必要时关闭CanMV或VS Code以释放COM7；关闭Arduino串口监视器释放COM9；随后双击`start_ballbeam_bridge.bat`。若BAT成功进入桥接且UNO的`frame`递增，则独立运行与转发链路成立；若提示COM7占用，则CanMV尚未释放端口。
- 安全边界：在`frame`递增且`vision_state=2`前不输入`ZERO`之后的运动参数，也不发送`ARM`。本次未操作串口或硬件，仅更新操作指导和日志。
- Git状态：本次仅追加操作日志，尚未提交。

## O-025：电脑桥接恢复TRACK并向UNO转发成功

- 日期：2026-08-01
- 对应条目：R07/R08电脑桥接现场复核
- 状态：K230D到UNO的视觉数据链再次跑通；UNO保持安全锁定，等待人工调平和`ZERO`。
- 用户提供的K230证据：桥接窗口持续显示约38 FPS、`STATE:TRACK`和`BG_OK`；本帧融合位置约`-11.26 cm`，霍夫通道约`-11.27 cm`。该片段中背景差分位置显示`BG:--`，但跟踪状态仍为TRACK；只记录现象，不据此推断背景通道失效原因。
- 用户提供的UNO证据：状态为`mode=LOCKED zero=0 frame=13073 pos_mm=-112 vision_state=2 track_age_ms=12`；UNO毫米位置与K230厘米位置一致，帧号已从此前的0更新，说明COM7严格帧已由电脑桥接转发到COM9并被UNO解析。
- 安全状态：`req_hz=0`、`current_hz=0`、`pulse=0`，`kp/ki/kd/sign/rate/accel/return_rate`全部为0；未满足ARM门，当前不会产生电机脉冲。
- 下一步：操作者先手工将梁调平，再在当前桥接控制台输入`ZERO`和`STATUS`，验收`zero=1`且`pulse=0`。PID参数和`ARM`需在下一阶段参数另行确认后执行。
- Git状态：本次仅追加用户提供的实测证据，尚未提交。

## O-026：说明通过BAT桥接窗口向UNO发送命令

- 日期：2026-08-01
- 对应条目：R08/R09电脑桥接交互
- 状态：操作方法已说明，等待用户执行`ZERO`和`STATUS`并回传输出。
- 用户问题：在K230和UNO日志持续滚动时，不清楚应在哪里以及如何输入命令。
- 操作说明：命令应输入到正在显示`[K230]`和`[UNO]`日志的黑色BAT桥接窗口，而不是CanMV终端或Arduino串口监视器。用鼠标聚焦该窗口后，以英文输入`ZERO`并回车，再输入`STATUS`并回车；日志滚动不影响桥接程序的标准输入。
- 验收输出：预期UNO返回`OK ZERO confirmed; beam_must_be_manually_level`，随后状态包含`mode=LOCKED zero=1`和`pulse=0`。若无该返回，不进入参数设置或`ARM`。
- 安全边界：必须先由现场操作者手工调平梁；本次仅提供操作说明并更新日志，未连接串口、未发送命令、未输出电机脉冲。
- Git状态：本次仅追加操作日志，尚未提交。

## O-027：关闭BAT中的K230调试日志刷屏

- 日期：2026-08-01
- 对应条目：R08/R09电脑桥接交互可用性
- 状态：BAT已修改，等待用户重启桥接后输入`ZERO`和`STATUS`。
- 用户现象：桥接窗口持续输出CanMV终端数据，键盘命令输入被滚动日志淹没，无法正常观察输入。
- 根因：`start_ballbeam_bridge.bat`启动桥接时使用了`--show-k230-log`，该选项会把所有未被识别为严格控制帧的K230人类可读日志输出到桥接窗口。
- 修改结果：从BAT启动命令中移除`--show-k230-log`。严格`B,...`控制帧仍会照常过滤并转发到UNO，只是不再显示K230人类可读调试行；UNO状态和命令回应继续显示。
- 使用方法：先在当前桥接窗口输入`quit`或按`Ctrl+C`退出，确认退出路径请求`DISARM`并关闭串口，再重新双击BAT。新窗口启动后，手工调平并输入`ZERO`、回车，再输入`STATUS`、回车。
- 安全边界：修改仅影响电脑窗口日志显示，不改变K230协议、UNO PID、电机脉冲或安全门；本次未打开串口、未发送命令、未输出电机脉冲。
- Git状态：BAT和日志修改尚未提交。

## O-028：增加UNO周期STATUS静默模式和命令提示符

- 日期：2026-08-01
- 对应条目：R08/R09电脑桥接交互可用性
- 状态：桥接程序和BAT已修改，等待用户重启后现场确认输入体验。
- 用户现象：关闭K230调试日志后，UNO固件每250 ms输出的周期性`STATUS`仍持续刷屏，无法正常观察键盘输入。
- 修改结果：`pc_serial_bridge.py`新增`--quiet-uno-status`选项。启用后显示UNO复位后的第一条状态，随后隐藏自动周期`STATUS`；用户输入`STATUS`时放行下一条状态。`ZERO`确认、错误、安全事件和其他非STATUS信息不隐藏。交互输入新增`[CMD] >`提示符。
- BAT集成：`start_ballbeam_bridge.bat`默认加入`--quiet-uno-status`，同时继续隐藏K230人类可读调试日志；严格视觉控制帧的过滤和转发不变。
- 验证结果：桥接单元测试由10项增至11项并全部通过；新增测试确认初始STATUS显示、后续周期STATUS隐藏且`OK ZERO`等非STATUS回应保留。桥接`--help`已显示新选项，`git diff --check`通过。
- 安全边界：本次只修改电脑端显示与交互，不更改UNO固件、PID参数、电机脉冲、安全限位或K230协议；未打开COM7/COM9、未发送硬件命令。
- Git状态：桥接、BAT、测试和日志修改尚未提交。

## O-029：说明ZERO与STATUS命令的作用边界

- 日期：2026-08-01
- 对应条目：R08/R09电脑桥接与安全门
- 状态：命令用途已说明，等待用户决定是否进入电机调试。
- 用户问题：询问在桥接窗口输入`ZERO`和`STATUS`的实际作用。
- `ZERO`作用：只允许在操作者已手工调平梁后使用；将UNO累计命令位置定义为0并设置`zero_confirmed`，从而满足后续ARM安全门之一。它不会驱动电机自动调平，不会读取X42S实际编码器，也不代表机械实际角度已被测量。
- `STATUS`作用：只读输出UNO当前模式、零位标志、视觉帧号和位置、TRACK帧龄、PID/速率参数及累计命令脉冲；不改变控制状态，不产生电机动作。
- 使用边界：若当前只验证K230D视觉和电脑桥接，可保持`LOCKED zero=0 pulse=0`，无需输入这两个命令。只有准备进入电机调试时，才按“手工调平→`ZERO`→`STATUS`确认`zero=1`和`pulse=0`”执行；PID参数和`ARM`仍需另行确认。
- 安全边界：本次仅解释命令语义并更新日志，未连接串口、未发送命令、未输出电机脉冲。
- Git状态：本次仅追加操作日志，尚未提交。

## O-030：人工零位确认和TRACK状态验收通过

- 日期：2026-08-01
- 对应条目：R09电机调试前安全门
- 状态：人工零位与视觉前置门已通过；电机仍锁定，等待下一阶段参数确认。
- 用户执行：在安静模式桥接窗口输入`ZERO`，UNO返回`OK ZERO confirmed; beam_must_be_manually_level`；随后输入`STATUS`。
- 状态证据：UNO返回`mode=LOCKED zero=1 frame=31937 pos_mm=-113 vision_state=2 track_age_ms=17`，说明人工零位标志已建立、视觉帧持续更新且TRACK数据新鲜。
- 安全证据：`req_hz=0`、`current_hz=0`、`pulse=0`，`kp/ki/kd/sign/rate/accel/return_rate`全部为0；当前没有电机输出，ARM安全门仍不会放行。
- 显示现象：`[CMD] >`与`[UNO] STATUS`出现在同一行是桥接主线程输入提示和UNO读取线程并发打印造成的显示交错，不代表协议帧错误或命令失败。
- 下一步边界：在用户确认进入受限电机调试及本轮参数前，不设置非零参数、不发送`ARM`。
- Git状态：本次仅追加用户提供的实测证据，尚未提交。

## O-031：提出PID分阶段整定与自动受限测试方案

- 日期：2026-08-01
- 对应条目：R09 PID实机调试
- 状态：整定顺序和安全工具建议已提出，等待用户确认后再实现或执行。
- 用户需求：当前视觉、桥接和人工零位均通过后，询问如何开始调PID。
- 当前事实：球位置约`-113 mm`，接近25 cm梁的左端；上一轮`SIGN=+1`、约+242命令脉冲时位置从-112变为-114 mm，未向中心改善并触发保护。该结果不足以单独确定极性错误，但不允许直接沿用原参数继续放大动作。
- 建议整定顺序（待用户确认）：先把球手工放到中心附近；只启用P并保持`KI=KD=0`；用与上一轮相反的`SIGN=-1`进行短时受限极性测试；只有位置绝对值持续减小时才逐级增加KP；出现往复振荡后再加入KD；KI最后根据稳态误差决定，允许继续保持0。
- 工具建议（待用户确认）：在电脑桥接程序增加本地`TEST <秒数>`命令，自动发送ARM并在到时后发送RETURN，同时基于真实K230位置监测相对基线是否恶化，恶化时提前RETURN。该功能用于减少人工输入RETURN的反应风险，不自动选择PID参数。
- 未执行项：尚未设置新的`SIGN/KP/KI/KD/RATE/ACCEL/RETURNRATE`，未发送`ARM`，未修改UNO固件，未输出电机脉冲。
- Git状态：本次仅追加操作日志，尚未提交。

## O-032：给出当前PID现场整定命令与判据

- 日期：2026-08-01
- 对应条目：R09 PID实机调试
- 状态：用户明确要求立即开始并自行把握现场风险；已给出本轮命令和后续调参顺序，等待用户执行结果。
- 当前前置状态：用户已提供`mode=LOCKED zero=1 frame=31937 pos_mm=-113 vision_state=2 track_age_ms=17 pulse=0`，视觉和人工零位门已通过。
- 本轮参数：沿用上一轮已经实际产生动作的`KP=2`、`KI=0`、`KD=0`、`RATE=200 pulse/s`、`ACCEL=400 pulse/s²`、`RETURNRATE=200 pulse/s`和`DEADBAND=5 mm`，只把控制方向从上一轮`SIGN=+1`改为`SIGN=-1`，用于确认相反方向是否使位置从约-113 mm向0 mm改善。这些仍是本轮测试参数，不是最终PID结论。
- 命令顺序：在LOCKED且zero=1时逐行输入`SIGN -1`、`KP 2`、`KI 0`、`KD 0`、`RATE 200`、`ACCEL 400`、`RETURNRATE 200`、`DEADBAND 5`、`STATUS`，核对后输入`ARM`。安静模式下可重复输入`STATUS`取得最新位置。
- 判定：若位置由-113向-100、-80、-50和0方向变化，则该极性方向成立并继续观察P控制；若位置变得更负，则输入`RETURN`并停止本轮。正常停止使用`RETURN`按累计命令位置回零；紧急停止使用`DISARM`，但其会使zero失效，后续必须重新手工调平和`ZERO`。
- 后续整定：方向确认后，P响应不足时按`KP=3`、`KP=4`逐级尝试；越过中心并往复振荡时回到较低KP并按`KD=0.5`、`1`、`2`逐级加入阻尼；只有PD稳定后仍存在固定稳态偏差时，才按`KI=0.02`、`0.05`、`0.1`逐级测试。每轮只改变一个参数并记录真实位置时间序列。
- 闭环边界：当前无独立梁角反馈，`pulse=0`只表示UNO累计命令位置回零，不证明X42S实际编码器或机械梁角已回零。现场梁未回人工水平时不得继续沿用zero。
- 未执行项：本次由Agent给出命令和判据，没有代用户打开串口或发送`ARM`，实际动作结果等待用户回传。
- Git状态：本次仅追加操作日志，尚未提交。

## O-033：SIGN=-1测试到达负向软件硬限位

- 日期：2026-08-01
- 对应条目：R09 PID实机调试
- 状态：测试已到达负向命令位置极限，已要求立即RETURN；等待用户提供回零证据。
- 前置状态：用户确认参数为`KP=2`、`KI=KD=0`、`SIGN=-1`、`RATE=200`、`ACCEL=400`、`RETURNRATE=200`，启动前`mode=LOCKED zero=1 pos_mm=-112 pulse=0 vision_state=2`。
- 用户输出：UNO接受`ARM`并返回`ARMED; PID waits for next TRACK frame`，随后输出`LIMIT blocked pulse=-1066`。
- 写实结论：位置误差约+112 mm时，P项约224 pulse/s，经200 pulse/s速率限幅并乘`SIGN=-1`后持续请求负向运动；UNO累计命令位置达到已配置的逆时针下限-1066脉冲，软件拒绝继续向负方向发脉冲。本次输出没有包含运动期间的球位置时间序列，因此不能仅凭限位信息判断球是否曾向中心改善。
- 发现的安全行为边界：当前`stepAllowed()`在到达硬限位时只把请求速率和当前速率清零并报告`LIMIT blocked`，不会自动从ARMED切换为RETURNING或LOCKED；操作者必须发送`RETURN`。该行为在后续继续PID调试前应考虑修改为触限自动RETURN，但本次未修改UNO固件。
- 立即操作：已要求用户输入`RETURN`，等待`RETURN complete; output_locked; zero_retained=1`，再输入`STATUS`确认`mode=LOCKED pulse=0`。若机械异常或RETURN无法正确回位，立即`DISARM`。
- 安全边界：在回零证据确认前，不继续修改PID、不再次ARM；本次Agent未代用户发送命令。
- Git状态：本次仅追加用户提供的实测证据，尚未提交。

## O-034：确认回零并增加运动状态采样显示

- 日期：2026-08-01
- 对应条目：R09 PID实机调试与电脑桥接观测
- 状态：当前已回到LOCKED命令零位；桥接显示逻辑已增强，等待重启桥接后进行下一轮。
- 用户回零证据：输入`RETURN`后UNO返回`RETURN begin reason=user from_pulse=0`和`RETURN complete; output_locked; zero_retained=1`；随后`STATUS`为`mode=LOCKED zero=1 frame=45795 pos_mm=-112 vision_state=2 track_age_ms=7 pulse=0`。
- 命令错误说明：用户随后输入了`mode=LOCKED`和`pulse=0`，UNO返回`ERR unknown_or_malformed_command`。这两个字符串是STATUS输出字段，不是可执行命令；错误拒绝符合解析器设计，没有改变控制状态。
- 未决现象：本轮先出现`LIMIT blocked pulse=-1066`，但用户发送RETURN时UNO报告`from_pulse=0`。现有日志没有覆盖两者之间的状态和球位置时间序列，因此不能确认命令位置是因PID反向驱动、自动保护还是其他已发生但未提供的过程回到0；不臆定根因。
- 桥接修改：安静模式继续隐藏LOCKED周期STATUS，但在`mode=ARMED`或`mode=RETURNING`时按0.5秒间隔显示实时STATUS；用户显式输入`STATUS`仍立即放行下一条。目的是在不恢复持续刷屏的情况下观察`pos_mm`、`req_hz`、`current_hz`和`pulse`动态。
- 验证结果：桥接单元测试由11项增至12项并全部通过；新增测试确认ARMED/RETURNING状态按0.5秒采样显示、间隔内重复状态被抑制；`git diff --check`通过。
- 安全边界：本次不修改UNO固件、PID参数、限位或脉冲输出；桥接需退出并重新启动后新显示逻辑才生效。Agent未发送硬件命令。
- Git状态：桥接、测试和日志修改尚未提交。

## O-035：记录SIGN=-1现场机械响应并确认梁端方向

- 日期：2026-08-01
- 对应条目：R09控制极性确认
- 状态：已收到部分现场反馈；“下面”所指梁端存在歧义，等待用户确认后决定下一轮方向。
- 用户反馈：本轮`SIGN=-1`时电机实际逆时针，现场描述为“下面”，钢珠没有移动。
- 已确认事实：`SIGN=-1`对应本次实物逆时针动作；钢珠在该轮没有产生可观察移动。结合此前命令位置曾到-1066，说明负向脉冲链路和机械动作存在，但不能据此认定球位置控制方向正确。
- 待确认歧义：用户所说“下面”尚不能确定是画面左端下降、右端下降，还是其他观察描述。由于钢珠当时约在-112 mm左端，若确为左端下降，则球继续停在左端与机械方向相符，并支持后续恢复`SIGN=+1`让左端升高；若是右端下降，控制极性结论相反。
- 下一步：已向用户确认“是否为水管左端下降、右端升高”。确认前不发送下一轮SIGN和PID参数，不再次ARM。
- 安全边界：本次仅解释现场反馈并更新日志，未操作串口或电机。
- Git状态：本次仅追加操作日志，尚未提交。

## O-036：实现连续自动闭环和BAT三参数输入

- 日期：2026-08-01
- 对应条目：R10连续自动闭环与三参数操作
- 状态：软件实现、电脑测试和UNO编译完成；等待用户烧录及实物闭环验收。
- 用户目标：无论钢珠从可识别范围内哪个位置开始，UNO都根据K230D位置误差运行PID、自动决定电机方向和速度，最终持续保持`0 mm`中心；后续操作只输入`KP/KI/KD`三个参数。
- 固定执行配置：依据此前已实际产生动作的配置，固化`SIGN=+1`、`RATE=200 pulse/s`、`ACCEL=400 pulse/s²`、`RETURNRATE=200 pulse/s`和`DEADBAND=5 mm`。这些是当前实现配置，不扩写为X42S规格或最终调参结论。
- UNO修改：新增单条`PID <kp> <ki> <kd>`命令，要求`KP>0`、`KI/KD>=0`且三者不超过10000；命令通过后启用自动控制。UNO复位时把启动姿态定义为累计命令零位，TRACK帧新鲜时自动ARM；误差正负经过固定控制极性自动选择CW/CCW；非TRACK或200 ms超时自动RETURN，TRACK恢复后自动重新ARM；触及+2133/-1066命令限位时关闭自动控制并自动RETURN，不再停留在ARMED限位状态。`RETURN`停止自动闭环并回命令零位，`DISARM`立即锁定并使零位失效。
- 零位边界：由于没有独立梁角反馈，启动零位仍只是假定操作者在BAT打开COM9并复位UNO之前已手工调平；它不是X42S实际编码器或机械梁角测量。BAT已明确提示先手工调平再输入三个增益。
- 桥接修改：新增`--pid KP KI KD`参数和三增益有限值/范围校验；线程启动后自动发送一条PID命令。运行中仍可在`[CMD] >`输入新的`PID kp ki kd`实时重新整定；安静模式在LOCKED时抑制周期STATUS，在ARMED/RETURNING时每0.5秒显示动态状态。
- BAT修改：设备、虚拟环境和脚本检查通过后，只提示并依次读取`KP`、`KI`、`KD`三个值，然后以`--quiet-uno-status --pid <KP> <KI> <KD>`启动桥接；不再要求用户输入ZERO、SIGN、RATE、ACCEL、RETURNRATE或ARM。
- 验证结果：Python协议、PID命令格式、非法增益、转发和状态显示共15项测试全部通过；桥接`--help`正确显示`--pid KP KI KD`；BAT使用不存在的COM999时正确拒绝并退出；`git diff --check`通过。UNO以`arduino:avr:uno --warnings all`编译通过，Flash 14046/32256 B（43%）、SRAM 462/2048 B（22%）；警告仅来自Arduino AVR Core自带`new.cpp`未使用参数，不来自本工程源码。
- 未验证项：本次未打开COM7/COM9、未烧录新UNO固件、未执行自动ARM或电机动作；从任意初始位置回中心、长期中心稳定、丢帧恢复和触限自动RETURN仍需用户烧录后实物验证，当前不能写为已跑通。
- Git状态：源码、桥接、BAT、测试和文档修改尚未提交。

## O-037：确认每次PID执行后的电机归零定义

- 日期：2026-08-01
- 对应条目：R10连续自动闭环
- 状态：用户提出新增归零需求；“执行一次”的结束条件存在歧义，等待确认后修改UNO程序。
- 用户需求：每次执行一次后把电机角度归零，并把计数清零。
- 控制边界：当前PID在每个TRACK帧上连续更新，不能把每一帧定义为一次并立即归零，否则每次倾角命令都会马上撤销，钢珠无法获得持续加速度。
- 建议定义（待用户确认）：钢珠进入中心死区（当前`±5 mm`）并连续稳定500 ms后，停止PID并进入RETURNING；UNO按累计命令脉冲反向回到命令零位，完成后显式把`command_position_pulses`清零，再自动进入下一轮中心保持。

## O-038：三参数输入后电机无动作的版本状态诊断

- 日期：2026-08-01
- 用户现象：运行BAT并输入`KP=2`、`KI=0`、`KD=0`后，电机没有动作。
- 源码核对：当前BAT通过`--pid 2 0 0`向UNO发送单条`PID 2 0 0`命令；当前工作区UNO源码收到该命令后才会启用自动闭环，并在新鲜TRACK到达时自动进入ARMED。
- 版本证据：O-036只记录了新UNO源码编译通过，尚未记录烧录和实物验证；因此当前没有证据表明UNO板内已运行三参数自动闭环固件。此前板上实测固件使用分开的`KP/KI/KD`、`SIGN/RATE/ACCEL/RETURNRATE`和`ARM`命令，不能由新BAT的单条`PID`命令启动。
- 写实结论：目前最直接的原因是电脑端BAT与UNO板内旧固件版本不匹配。需先退出桥接、关闭占用COM9的软件，把当前`BallBeam_UNO.ino`烧录到UNO，再重启BAT并以`OK PID`、`AUTO ARMED`以及ARMED状态中的非零`req_hz/current_hz`作为验收证据。
- 本次边界：只诊断和记录，没有打开COM7/COM9、没有烧录，也没有输出电机脉冲。

## O-039：自动ARM后命令计数增长但电机几乎不动

- 日期：2026-08-01
- 用户证据：新固件已进入`mode=ARMED zero=1 auto=1`；在`KP=5`、`KI=KD=0`时，`req_hz=current_hz=200`，命令计数从`+726`增长到`+1197`，视觉位置约从`-80 mm`恶化到`-105 mm`，用户观察电机几乎没有动作。
- 软件侧结论：约2.5秒增加471个命令脉冲，与200 pulse/s基本一致，说明PID、速率限幅和UNO命令脉冲计数路径正在运行；`pulse`仍只是UNO命令累计量，不是X42S实际编码器位置。
- 速度换算边界：按用户此前确认的默认16细分、3200脉冲/圈，200 pulse/s相当于命令3.75 rpm，即约22.5°/s；单个0.5秒状态周期约11.25°命令角，因此瞬时观察会显得慢。但累计1197脉冲约为0.37命令圈，若实际角度明显小于该值，仅用“速度较低”不能解释。
- 待确认根因：X42S实物`P_Pul`、`CtrlMode`、`En`仍未记录。若实物为`PUL_WID`，当前50%占空比计数脉冲会被解释为脉宽位置命令，而不是逐沿累计的标准PUL/STEP；这可导致UNO计数持续增长但电机不按计数位移。该项是依据厂商资料提出的待确认可能性，不写成已确认根因。
- 方向证据：同一段中视觉位置从约`-80 mm`变得更负至`-105 mm`，当前`sign=+1`动作没有形成向0改善的证据；在模式未确认前不继续提高固定速度或增益。
- 立即处置：已要求操作者先`RETURN`；若位置继续恶化或机构状态异常，应使用`DISARM`立即锁定，再人工调平。下一步读取并反馈X42S菜单`P_Pul`、`CtrlMode`、`En`的完整显示值。
- 本次边界：只读取源码和厂商资料基线、诊断并记录；没有打开串口、没有改动控制源码或烧录固件。

## O-040：分层说明命令计数增长但实际运动很小的可能原因

- 日期：2026-08-01
- 用户要求：描述当前遇到的情况以及推断可能发生的原因。
- 已确认现象：UNO处于`ARMED/auto=1`，K230D TRACK帧新鲜；`KP=5`、位置约-80 mm时P项超过固定200 pulse/s上限，因此`req_hz/current_hz`均饱和为+200；约2.5秒内软件命令计数由+726增至+1197，与输出调度相符；用户观察电机几乎不动；视觉位置随后变得更负至约-105 mm。
- 已确认边界：视觉、电脑桥接、UNO解析、PID计算、ARM状态和软件脉冲调度链路已经运行；`pulse`只是UNO每次把D3置高时增加的命令计数，不是X42S内部编码器实际角度，也不能证明驱动器接收并执行了每个脉冲。
- 原因排序：第一，200 pulse/s在3200脉冲/圈下仅为3.75 rpm，短时间看起来较慢，但累计1197脉冲约134.7°命令角，若实际远小于此值则速度低不是完整解释。第二，实物可能处于`PUL_WID`而非标准计数脉冲模式，当前50%占空比波形会被解释为重复的脉宽位置命令。第三，`P_Pul`关闭、`CtrlMode`或`En`设置与脉冲端口不匹配。第四，信号共地/电平/接线接触问题导致UNO计数但X42S漏收；当前只有方向编号曾验证，准确一圈和自动停止未取得独立验收。第五，电机轴到球杆之间可能存在减速、间隙、打滑或机构阻力，导致轴运动没有等量传到球杆。
- 控制方向边界：视觉位置从-80 mm到-105 mm表明本段没有朝0改善，但尚不能只据此唯一认定`sign=+1`错误；还可能涉及驱动模式、实际梁角太小、机械传动、球静摩擦或视觉变化。模式与实际运动量确认前不通过继续增大KP判断方向，因为当前P输出已经饱和。
- 下一判据：读取X42S的`P_Pul`、`CtrlMode`、`En`实值；随后用已知有限脉冲数对比电机实际角变化，才能区分“软件速率低”“输入模式错误”“脉冲未接收”和“机械传动损失”。
- 本次边界：只整理证据和推断并写入日志，没有更改源码、打开串口、烧录或输出电机脉冲。

## O-041：确认X42S标准脉冲、闭环和一直使能菜单

- 日期：2026-08-01
- 用户实物确认：X42S菜单显示`P_Pul=PUL_ENA`、`CtrlMode=CR_VFOC`、`En=HOLD`。
- 厂商资料核对：`PUL_ENA`表示脉冲端口可接收输入信号；`CR_VFOC`对应当前使用的内部闭环职责；`HOLD`在厂商参数表中表示一直使能。因此此前怀疑实物处于`PUL_WID`脉宽位置模式的可能性已排除，当前不配置UNO EN输出不会阻止运动。
- 当前最关键未知量：实物`MStep`。当前UNO硬编码按资料默认16细分、3200脉冲/圈计算；若实物为更高细分，相同200 pulse/s和1197累计命令脉冲对应的转速、角度都会按比例更小，可以解释“电机几乎没动”的一部分或全部现象。
- 后续判据：读取`MStep`完整显示值。如果为16，1197命令脉冲应约为134.7°电机命令角，实际明显小于该值时继续检查X42S接收脉冲数、电气信号和机械传动；如果不是16，先按实值重算每圈脉冲、速度和+2/3/-1/3圈软件限位，再改程序。
- 文档同步：已更新README、开发跟踪、工作条目、引脚映射、ZDT资料映射，以及DS2026的张大头控制基线和子工程索引，删除`P_Pul/CtrlMode/En`仍未知的旧状态，只保留`MStep`待确认。
- 本次边界：没有改动UNO控制源码、打开串口、烧录或输出电机脉冲。

## O-042：确认实物16细分并排除换算原因

- 日期：2026-08-01
- 用户实物确认：X42S菜单`MStep=16`。
- 确认换算：厂商资料说明默认1.8°步进电机在16细分下每个脉冲为0.1125°，3200脉冲为360°；当前实物菜单与该条件一致。因此200 pulse/s对应22.5°/s，`pulse=1197`对应约134.7°电机轴命令角。
- 排除项：此前“实物细分高于16导致动作很小”的可能性排除；`PUL_WID`、脉冲端口关闭、EN未使能也已由`PUL_ENA/CR_VFOC/HOLD`排除。厂商资料标明脉冲输入支持3.3–24V，UNO 5V输出电平本身处于支持范围。
- 当前分叉：若用户所说“几乎没动”指电机轴，则UNO命令计数与X42S实际执行不一致，应核对X42S收到的输入脉冲数、目标位置、实时位置以及共地/接触/堵转状态；若指电机轴已转但球杆几乎不动，则优先检查齿轮、联轴器、间隙、打滑、传动比例和机构阻力。
- 控制算法观察：当前P输出是有符号脉冲速率，误差存在时会持续累加电机位置直至球移动、误差反向或触及限位；后续是否改为“PID输出目标梁角/目标脉冲位置，再限速跟随”的结构，需结合电机轴和球杆实际运动确认。
- 文档同步：UNO工程已把R02标为完成并同步README、开发跟踪、工作条目、平台映射、引脚映射和ZDT资料映射；DS2026已同步控制基线和子工程索引。
- 本次边界：没有改动控制源码、打开串口、烧录或输出电机脉冲。

## O-043：确认电机轴实际几乎不转并比较新旧脉冲时序

- 日期：2026-08-01
- 用户现场确认：“几乎没动”指X42S电机轴本身几乎没有转动，不是电机轴已转而球杆传动量很小。
- 已排除范围：K230D视觉、电脑桥接、UNO自动ARM、PID计算、200 pulse/s软件调度、`PUL_ENA/CR_VFOC/HOLD/MStep=16`菜单、16细分角度换算和机械传动比均不能解释“UNO累计1197命令脉冲而电机轴几乎不转”。
- 新旧源码比较：R03一圈测试与当前PID源码均使用D2方向、D3计数脉冲、方向设置后10 ms等待和50%占空比方波；200 Hz时当前程序高/低电平各约2.5 ms。旧程序在下降沿计数、当前程序在上升沿计数，但两者每个完整周期都只累计一次，未发现能把1197软件计数缩成少量物理脉冲的时序差异。
- 厂商电气依据：手册说明脉冲信号支持3.3–24V，因此UNO 5V电平范围本身符合；光耦版本另有共阳NPN接法，当前实物是否为光耦版本、是否按对应接法和是否共地仍需以实物接线确认。
- 当前最高价值诊断：在X42S侧读取输入脉冲数、目标位置、实时位置、位置误差和状态标志。若输入脉冲数没有随UNO命令增加，定位D3实际波形、接线/共地或输入类型；若输入脉冲数增加但实时位置不跟随，定位电机供电、校准、堵转/保护或闭环执行问题。
- 厂商只读能力：Emm自由协议例程提供`0x32`读取输入脉冲数、`0x33`目标位置、`0x36`实时位置、`0x37`位置误差和`0x3A`状态标志；MODBUS手册也提供对应只读寄存器。是否接入需要先确认实物通讯版本和物理接口，不擅自接线或发送。
- 本次边界：没有修改控制源码、打开串口、烧录、改菜单或输出电机脉冲。

## O-044：汇总当前对话、程序路径和联调状态

- 日期：2026-08-01
- 用户要求：总结本轮对话、更新开发文档，并列出已经编写程序的完整路径。
- 新增文档：`doc/当前联调总结.md`，集中记录系统数据链、已确认硬件/菜单、全部程序路径、能跑通和不能跑通的功能、已排除原因、当前故障分支、下一步只读诊断以及控制结构边界。
- 当前写实状态：K230D自启动、COM7严格帧、PC桥接、UNO视觉解析、三参数自动ARM和200 pulse/s软件命令计数均已跑通；实物为`PUL_ENA/CR_VFOC/HOLD/MStep=16`。但UNO累计1197命令脉冲时，用户确认X42S电机轴本身几乎没有转动，钢球中心稳定尚未实现。
- 未完成需求：用户提出的“每次执行完成后电机角度归零并且计数清零”尚未写入源码；建议完成条件为中心±5 mm连续500 ms，仍等待用户确认，因此新增R11并保持等待状态。
- 程序路径：K230D源码`C:\Users\Y_cheng\Desktop\DS_26\k230_try.py`（板内为`/sdcard/main.py`）；UNO源码`C:\Users\Y_cheng\Documents\Arduino\BallBeam_UNO\BallBeam_UNO.ino`；启动BAT`C:\Users\Y_cheng\Documents\Arduino\BallBeam_UNO\start_ballbeam_bridge.bat`；PC桥`C:\Users\Y_cheng\Documents\Arduino\BallBeam_UNO\tools\pc_serial_bridge.py`；测试`C:\Users\Y_cheng\Documents\Arduino\BallBeam_UNO\tests\test_pc_serial_bridge.py`。
- 验证：使用工程`.venv`运行电脑桥接单元测试，15项全部通过；使用Arduino IDE内置`arduino-cli`按`arduino:avr:uno --warnings all`重新编译当前源码，Flash 14046/32256字节（43%），SRAM 462/2048字节（22%），编译通过。
- 文档同步：更新README当前进度和总结入口、开发跟踪R05/R06/R10/R11、工作条目R10/R11，以及DS2026子工程索引。
- 本次边界：只新增/更新文档并执行本地测试和编译；没有打开COM7/COM9、烧录、改X42S菜单或输出电机脉冲。

## O-045：请求把后续使用的程序路径放到桌面

- 日期：2026-08-01
- 用户要求：把将来要使用的已编写程序路径放到桌面上。
- 当前歧义：尚未确认是创建一个集中列出完整路径的桌面文本文件，还是分别创建可直接打开/运行UNO源码、启动BAT、PC桥和K230D源码的桌面快捷方式。
- 待确认：建议创建`桌面\BallBeam程序入口`文件夹，内含一份路径说明和四个快捷方式；用户确认形式后执行。
- 本次边界：只记录需求，尚未在桌面创建、覆盖或删除任何文件。

## O-046：创建桌面程序入口文件夹和快捷方式

- 日期：2026-08-01
- 用户确认：采用“文件夹加快捷方式”的桌面入口形式。
- 创建目录：`C:\Users\Y_cheng\Desktop\BallBeam程序入口`；该目录此前不存在，因此没有覆盖既有桌面文件。
- 创建内容：`01_启动BallBeam桥接.lnk`、`02_UNO主程序.lnk`、`03_PC串口桥源码.lnk`、`04_K230D视觉程序.lnk`、`05_当前联调总结.lnk`、`06_UNO工程文件夹.lnk`、`07_DS2026文档仓库.lnk`，以及`程序路径与使用说明.txt`。
- 使用边界：01号快捷方式直接运行现有启动BAT；02打开UNO源码；03和04只在资源管理器中定位Python源码，避免无参数误运行PC桥或在电脑端误运行K230脚本；05打开总结；06至07打开工程目录。没有移动或复制原程序，原工程路径保持不变。
- 验证：逐个读取7个Windows快捷方式，目标文件或`explorer.exe`均存在，工作目录均存在；本次没有实际启动BAT、打开COM7/COM9、烧录或输出电机脉冲。
- 角度事实边界：当前没有X42S实际编码器或独立梁角反馈，因此“电机角度归零”只能表示按UNO累计命令脉冲反向回零，不能写成已读取并确认实际电机角或机械梁角为0。
- 未执行项：尚未修改UNO源码、未编译、未烧录、未发送电机命令；等待用户确认中心死区和稳定时间。
- Git状态：本次仅追加操作日志，尚未提交。
